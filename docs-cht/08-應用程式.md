# 原生應用程式

## 概述

OpenClaw 提供 iOS、Android、macOS 三個原生應用程式，都透過 WebSocket 連接 Gateway 伺服器進行通訊。

## 應用程式架構

```
                    ┌─────────────────────────────────────┐
                    │            Gateway                  │
                    │         (WebSocket Server)          │
                    └─────────────────────────────────────┘
                         ▲              ▲              ▲
                         │              │              │
              WebSocket  │   WebSocket  │   WebSocket  │
                         │              │              │
               ┌─────────┴───┐   ┌──────┴──────┐   ┌───┴─────────┐
               │   macOS     │   │    iOS      │   │   Android   │
               │    App      │   │    App      │   │     App     │
               │             │   │             │   │             │
               │ - Menu Bar  │   │ - Node Mode │   │ - Node Mode │
               │ - Local GW  │   │ - Camera    │   │ - Camera    │
               │ - Chat UI   │   │ - Voice     │   │ - SMS       │
               │ - Canvas    │   │ - Location  │   │ - Screen    │
               └─────────────┘   │ - Watch App │   └─────────────┘
                                 └─────────────┘
```

## iOS 應用

### 目錄結構

```
apps/ios/
├── project.yml              # XcodeGen 專案定義
├── Sources/                 # 原始碼
│   ├── Info.plist           # 應用程式資訊
│   ├── Gateway/             # Gateway 連接
│   ├── Onboarding/          # 設定流程
│   ├── Screen/              # 螢幕功能
│   ├── Voice/               # 語音功能
│   ├── Services/            # 服務
│   └── EventKit/            # 事件套件
├── WatchApp/                # Apple Watch 應用
├── WatchExtension/          # Watch 擴充
└── Tests/                   # 測試
```

### 主要功能

| 功能 | 說明 |
|------|------|
| Node Mode | 作為節點連接 Gateway，提供裝置能力 |
| Camera | 拍照並傳送給 AI |
| Voice Wake | 語音喚醒功能（guard 回呼安全、incremental speech pacing） |
| Location | 提供位置資訊 |
| Canvas | WebView 顯示 A2UI |
| Watch App | Apple Watch 整合（session activation 並發安全） |
| Setup Code | 配對碼功能（短期單次使用 bootstrap tokens） |
| Welcome Pager | 首次啟動歡迎導覽頁（2026.3.13+） |
| Home Canvas | 全新首頁 canvas 工具列（2026.3.13+） |
| APNs Relay | iOS APNs relay gateway（2026.3.13+） |
| Beta Release | 本地 beta release flow（2026.3.13+） |
| Keychain | Gateway metadata/TLS fingerprints 裝置 keychain 儲存 |
| Concurrency | 相機/Gateway 連線路徑 lock-protected 存取 |
| Security | 限制 API-key 來源為 app config；deep-link 確認 coalesce |
| TTS Fallback | PCM→MP3 降級（provider 格式不支援時） |
| Live Activity | 連線狀態顯示 + 過期清理 |
| App Store | App Store Connect 發布資產準備 |
| Foreground Resume | 前景恢復時安全重播佇列 actions |
| Gateway Reconnect | 前景返回時自動重連 gateway |
| Quick Setup Skip | 已配置 gateway 時跳過 quick setup |
| Canvas Auto-load | 自動載入 scoped gateway canvas |
| ActivityKit Preconcurrency | ActivityKit 匯入加入 @preconcurrency 標記（2026.3.31+） |
| Exec Approval Notification | 執行核准通知流程，支援 push notification 核准/拒絕（2026.4.5+） |
| A2UI Trusted URL | Canvas A2UI 限制至受信任 gateway URL（2026.4.5+） |
| Gateway Error UX | 改善 Gateway 連線錯誤的使用者體驗（2026.4.8+） |
| Watch Exec Approval | 加強 Apple Watch exec approval 審核流程（2026.4.8+） |

### 狀態管理

```swift
// 使用 Observation 框架（推薦）
@Observable
class NodeAppModel {
    var connectionState: ConnectionState = .disconnected
    var gatewayUrl: String?
    var capabilities: NodeCapabilities
    
    func connect() async { /* 連接 Gateway */ }
    func handleInvoke(_ request: NodeInvokeRequest) async -> NodeInvokeResult {
        /* 處理節點呼叫 */
    }
}
```

### 建置與執行

```bash
# 生成 Xcode 專案
cd apps/ios && xcodegen generate

# 開啟專案
open OpenClaw.xcodeproj

# 建置
pnpm ios:build

# 執行
pnpm ios:run
```

**注意**: 優先使用已連接的真實裝置（iOS + Android），再使用模擬器。

## Android 應用

### 目錄結構

```
apps/android/
├── app/
│   ├── build.gradle.kts         # 應用程式建置配置
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/ai/openclaw/app/
│       │   ├── MainActivity.kt
│       │   ├── MainViewModel.kt
│       │   ├── NodeRuntime.kt
│       │   ├── gateway/
│       │   ├── ui/
│       │   └── capabilities/
│       └── res/
├── build.gradle.kts
└── settings.gradle.kts
```

### 主要功能

| 功能 | 說明 |
|------|------|
| Node Mode | 作為節點連接 Gateway |
| Camera | 拍照功能、`camera.list` 相機列表 |
| Screen Recording | 螢幕錄製（`durationMs` 上限 5 分鐘；前台服務已移除） |
| SMS | 簡訊存取（需權限） |
| Voice | 語音輸入、ElevenLabs WebSocket TTS 串流 |
| Location | 位置服務 |
| Notifications | 通知操作（`open`/`dismiss`/`reply`）、`notifications.changed` 事件 |
| Device | `device.permissions`、`device.health` 裝置狀態 |
| Contacts | `contacts.search`/`contacts.add` 聯絡人管理 |
| Calendar | `calendar.events`/`calendar.add` 行事曆管理 |
| Motion | `motion.activity`/`motion.pedometer` 動態感測器 |
| Photos | `photos.latest` 最新照片 |
| System | `system.notify` 系統通知 |
| Onboarding UI | 全新 onboarding flow UI 重設計（2026.3.13+） |
| Chat Settings | 分組的裝置和媒體設定重設計（2026.3.13+） |
| Voice Tab | Speaker label 和 status pill 更新（2026.3.13+） |
| Connect Tab | 統一狀態卡片重設計（2026.3.13+） |
| Google Code Scanner | 替換 legacy ZXing 掃描器（2026.3.13+） |
| Canvas Bridge 安全 | 限制 canvas bridge 至受信任頁面（2026.3.23+） |
| Bitmap 記憶體修復 | 修復 PhotosHandler、CanvasController、CameraCaptureManager 洩漏（2026.3.23+） |
| Sensor Race Fix | 修復 MotionHandler sensor callback 競態（2026.3.23+） |
| Permission Dialogs | 強化 permission dialog 跨 activity 拆卸（2026.3.23+） |
| Contacts SQL Fix | 修復 SQL LIKE wildcard escaping（2026.3.23+） |
| Location Callback | 修復 cancellation-safe current-location callback（2026.3.23+） |
| Status Bar | 更新 OpenClawTheme 狀態列外觀（2026.3.23+） |
| Gateway Setup | 使用 scheme 預設 port（2026.3.23+） |
| SMS Search | 支援 node sms.search（2026.3.23+） |
| Play Builds | 隱藏受限能力在 Play builds 中（2026.3.23+） |
| Benchmark | 新增 benchmark script（2026.3.23+） |
| SMS Search 增強 | sms.search 支援更豐富的搜尋功能（2026.3.31+） |
| Notification Forwarding | 通知轉發至 gateway 事件流（2026.3.31+） |
| Voice Silence Auto-send | 語音靜默自動送出（2026.3.31+） |
| Google Assistant | Google Assistant App Actions 整合，語音指令 entrypoint（2026.4.5+） |
| Auto-send Prompts | Assistant 語音指令自動送出 prompts（2026.4.5+） |
| Talk Speak Playback | talk.speak 播放路徑支援（2026.4.5+） |
| TLS Remote Gateway | 遠端 gateway 連線強制 TLS（2026.4.5+） |
| Cleartext LAN | 私有 LAN 位址允許 cleartext ws://（2026.4.5+） |
| Talk Reply TTS | Talk 模式回覆 TTS 恢復播放（2026.4.5+） |
| In-flight Cancellation | Talk 播放中可取消進行中的音訊（2026.4.5+） |
| Mobile Approval Card | 行動裝置核准卡片改善：最大高度、彈性佈局、可捲動預覽（2026.4.8+） |
| Safe Area Inset | 行動裝置核准覆蓋層底部安全區域 inset（2026.4.8+） |

### 建置與執行

```bash
# 建置 Debug APK
pnpm android:assemble

# 安裝到裝置
pnpm android:install

# 建置並執行
pnpm android:run

# 執行測試
pnpm android:test
```

## macOS 應用

### 目錄結構

```
apps/macos/
├── Sources/OpenClaw/
│   ├── MenuBar.swift            # 選單列入口 (@main)
│   ├── AppState.swift           # 應用程式狀態
│   ├── ControlChannel.swift     # 控制頻道
│   ├── GatewayProcessManager.swift
│   ├── Views/
│   │   ├── ChatView.swift
│   │   ├── SettingsView.swift
│   │   └── CanvasWindow.swift
│   ├── Node/
│   │   └── MacNodeModeCoordinator.swift
│   └── Resources/
│       └── Info.plist
├── Package.swift
└── Tests/
```

### 主要功能

| 功能 | 說明 |
|------|------|
| Menu Bar | 選單列常駐應用 |
| Local Gateway | 運行本地 Gateway |
| Remote Gateway | 連接遠端 Gateway |
| Chat UI | 聊天介面 |
| Canvas | A2UI 視窗 |
| Voice Wake | 語音喚醒 |
| Node Mode | 同時作為節點 |
| Remote Token | 遠端模式 gateway token 欄位 |
| Chat Model Selector | 聊天模型選擇器 + thinking 持久化（2026.3.13+） |
| Remote Auth Prompt | onboarding 提示 remote gateway auth tokens（2026.3.13+） |
| Exec Approvals | 遵從 gateway prompter 中的 per-agent exec approval 設定（2026.3.13+） |
| Tailscale | 改善 Tailscale gateway discovery |
| Universal | release 預設 universal binaries |
| PortGuard | 防止殺死 remote mode 的 Docker Desktop（2026.3.13+） |
| Node.js 版本 | 最低版本對齊至 22.16.0（2026.3.13+） |
| Reminders | NSRemindersUsageDescription 權限（2026.3.13+） |
| macOS Publishing | 自動化 macOS 發布流程（2026.3.23+） |
| MagicDNS Discovery | Tailscale MagicDNS gateway 自動發現（2026.3.31+） |
| Launch Agent Recovery | launchd Launch Agent 恢復機制（2026.4.5+） |
| KeepAlive Restarts | launchd KeepAlive 自動重啟 gateway（2026.4.5+） |
| PID Recycling Detection | 偵測 PID 回收避免誤殺不相關進程（2026.4.5+） |
| Windows Self-restart | Windows 平台 gateway 自我重啟清理（2026.4.5+） |
| CLI Version Output | CLI 版本輸出移除 commit hash（2026.4.8+） |

### 建置與執行

```bash
# 打包應用程式
pnpm mac:package

# 開啟應用程式
pnpm mac:open

# 重啟（開發用）
pnpm mac:restart
# 或使用腳本
scripts/restart-mac.sh
```

**注意**: 不要透過 SSH 重新建置 macOS 應用；必須直接在 Mac 上執行。

### Gateway 管理

macOS 應用內建 Gateway 管理。Gateway 目前僅作為選單列應用執行，沒有獨立的 LaunchAgent。
- 透過 OpenClaw Mac app 啟動/停止
- 驗證/查看進程：`launchctl print gui/$UID | grep openclaw`
- 支援 launchd KeepAlive 自動重啟（2026.4.5+）
- PID 回收偵測：避免進程管理誤殺不相關進程（2026.4.5+）
- Launch Agent 恢復：異常退出後自動恢復 gateway 服務（2026.4.5+）

## 共享程式碼

### OpenClawKit

```
apps/shared/OpenClawKit/
├── Sources/OpenClawKit/
│   ├── GatewayChannel.swift
│   ├── TalkMode.swift
│   ├── CanvasHost.swift
│   └── SystemCommands.swift
├── Sources/OpenClawProtocol/
│   ├── GatewayModels.swift      # 協議模型（自動生成）
│   └── Messages.swift
└── Tests/
```

### 協議模型

```swift
// GatewayModels.swift（自動生成）
struct ConnectRequest: Codable {
    let role: NodeRole
    let token: String?
    let capabilities: NodeCapabilities?
    let commands: [NodeCommand]?
}

enum NodeRole: String, Codable {
    case control
    case node
    case chat
}
```

## 裝置能力

### iOS 能力

```swift
struct NodeCapabilities: Codable {
    var camera: Bool = true
    var microphone: Bool = true
    var location: Bool = true
    var screenshot: Bool = false  // iOS 限制
    var tts: Bool = true
}
```

### Android 能力

```kotlin
data class NodeCapabilities(
    val camera: Boolean = true,
    val microphone: Boolean = true,
    val location: Boolean = true,
    val screenshot: Boolean = true,
    val screenRecording: Boolean = true,
    val sms: Boolean = true,
    val tts: Boolean = true
)
```

### macOS 能力

```swift
struct NodeCapabilities: Codable {
    var camera: Bool = true
    var microphone: Bool = true
    var screenshot: Bool = true
    var tts: Bool = true
    var exec: Bool = true
}
```

## 開發指南

### 協議同步

```bash
# 生成 TypeScript 協議
pnpm protocol:gen

# 生成 Swift 模型
pnpm protocol:gen:swift
```

### SwiftUI 最佳實踐

```swift
// 使用 Observation 框架（推薦）
@Observable
class MyModel {
    var value: String = ""
}

// 不要使用 ObservableObject（除非相容性需要）
// class MyModel: ObservableObject { ... }
```

觸碰相關程式碼時，應將現有 `ObservableObject` 遷移至 `@Observable`。

### 版本位置

| 平台 | 檔案 |
|------|------|
| CLI | `package.json` (version) |
| Android | `apps/android/app/build.gradle.kts` (versionName/versionCode) |
| iOS | `apps/ios/Sources/Info.plist` (CFBundleShortVersionString/CFBundleVersion) |
| macOS | `apps/macos/Sources/OpenClaw/Resources/Info.plist` |

### 測試

```bash
# iOS 測試
cd apps/ios && xcodebuild test ...

# Android 測試
pnpm android:test

# macOS 測試
cd apps/macos && swift test
```

**重啟應用** = 重新編譯/安裝並重新啟動（不只是 kill/launch）。

---

## 應用程式改善（2026.4.12）

### iOS

- **CalVer 版本號**：iOS 版本號改用 CalVer 格式（與專案版本一致）
- **Talk Mode Permission 修復**：修復 Talk Mode 麥克風權限請求問題

### macOS

- **Chokidar Glob 修復**：修復 macOS 上 chokidar 檔案監視的 glob 模式問題
- **Gateway Restart Script**：改進 `scripts/restart-mac.sh` gateway 重啟流程


---

## 應用程式改善（2026.4.13–2026.4.14）

### Control UI

- **ReDoS 修復**（#46707）：將 marked.js 替換為 markdown-it，修復惡意製作的 markdown 可能透過 ReDoS 凍結 Control UI 的漏洞
- **Session 保留**（#59611）：重連和 tab 切換時保留使用者選擇的 session
- **Dreaming Wiki 防護**：防護 dreaming wiki plugin 呼叫避免空指標錯誤

### macOS Gateway

- **Parallels 測試加強**：macOS Parallels gateway 測試逾時延長和改善
- **Canvas Auth**：canvas auth helper 等待時間限制

## 應用程式改善（2026.4.15）

### Control UI

- **Model Auth 狀態卡片**（#66211）：Overview dashboard 新增 Model Auth 狀態卡片，顯示 OAuth token 健康狀態和 provider rate-limit 壓力
- **Model Auth 假陽性修復**（#67253）：修復已別名化 providers、env-backed OAuth 和無法解析 env SecretRefs 的假陽性「缺少」警示
- **Active Sends 期間保留訊息**（#66997）：保留樂觀使用者訊息卡片在 active sends 期間，延遲 history reloads 至 active run 結束
- **Exec Approval Modal**（#67082）：約束 exec approval modal overflow，防止長命令將操作按鈕推出視野

### Android

- **WebView 現代化**（#67627）：Android WebView 和 discovery API 使用現代化，提升相容性和效能

### 依賴安全更新

- **Hono 升級**（#67613）：hono 升至 4.12.14，@hono/node-server 升至 1.19.14（修復 GHSA-458j-xx4x-4375）
- **DOMPurify 升級**（#67614）：DOMPurify 升至 3.4.0，加強 XSS 防護
- **follow-redirects 修補**（#66649）：修補 follow-redirects 安全漏洞

## 應用程式改善（2026.4.18–2026.4.20）

### Control UI Settings 全面改版（2026.4.18）

- **Settings 體驗改版**（#67819）：Settings 和 slash-command 體驗全面翻新，包含更快速的 presets、quick-create flows 和重新設計的 command discovery
- **Settings Scroll 重置**（#68150）：切換 settings 頁面時重置 scroll 位置，對齊 details headers

### Device Pairing UI 改善（2026.4.20）

- **Scope/Role Approval 說明**（#69221）：重連期間解釋 scope 和 role approval upgrades，Control UI 顯示 requested vs approved access
- **Pairing Approvals 顯示**（#69226）：Control UI 顯示待處理的 scope、role 和 device-metadata 配對審批，避免 broader reconnects 看起來像隨機 auth 中斷

### Webchat 改善（2026.4.20）

- **Pure-Image Turn 修復**（#69474）：webchat image-only turns 視為 media 處理，不被 empty-turn gating 阻止
- **macOS Webchat Undo/Redo**（#34962）：macOS webchat composer 文字輸入啟用 Undo 和 Redo

### macOS 改善（2026.4.18）

- **Screen Snapshot 支援**（#67954）：macOS app nodes 新增 `screen.snapshot` 支援，包含 runtime plumbing 和預設 macOS allowlisting
- **Microphone 存取**：Permissions-Policy header 允許同 origin 麥克風存取
- **Remote SSH StrictHostKeyChecking**：切換至 `StrictHostKeyChecking=yes`，SSH 連接需要預先信任的 host key

### Setup TUI 改善（2026.4.20）

- **Hatch Terminal Handoff**（#69524）：Setup hatch TUI 在新 process 重啟，保留已配置的 gateway target 和 auth source，不在命令列 args 上暴露 gateway secrets

### Onboarding Wizard 改善（2026.4.20）

- **安全聲明改版**：黃色警告橫幅 + 章節標題 + 清單格式，關鍵指引清晰可掃描
- **Loading Spinner**：模型 catalog 載入期間顯示 loading spinner，wizard 不再出現空白
- **API Key Placeholder**：provider API key 輸入框新增 "API key" placeholder
- **可搜尋選取**：wizard 支援 searchable select

### 依賴安全更新（2026.4.18）

- **Hono 升級**：修復 GHSA 安全問題（來自 2026.4.15 的持續追蹤）
- **DOMPurify 升級**：XSS 防護強化
- **Android WebView 現代化**（延續 2026.4.15）：Android WebView 相容性持續提升

## 應用程式改善（2026.4.22–2026.5.2）

### Control UI（2026.4.22–2026.4.23）

- **操作者身分**（2026.4.22）：瀏覽器本機個人姓名 + 頭像（本機安全路徑），與共用聊天頭像組件對齊
- **設定與版面**（2026.4.22）：Quick Settings、agent fallback chips、窄螢幕聊天防裁切／節省空間
- **上下文百分比**（2026.4.22）：訊息 footer **納入 cache read／write tokens**，避免有快取時仍顯示 `0% ctx` 與 `/status` 不一致
- **助理生成圖**（2026.4.23）：持久化為 **受管 media**；**paired device token** 可拉取 assistant media；歷史重載仍顯示生成圖
- **Stop 按鈕**（2026.4.23）：Abort **跨 Gateway 重連排隊**，重連後仍取消進行中 run
- **`/usage`**（2026.4.24）：採 **較新 context snapshot**；**cache-write tokens** 納入快取命中率分母（與概覽一致）。  
- **Bundle**（2026.4.24）：Control UI／瀏覽器 profile facade 匯出保持 **browser-safe**，避免將僅適用 Node 的模組打進前端包。

### Control UI／Android（2026.4.25）

- **PWA**：支援 **安裝為 PWA**；**Web Push**：Gateway 聊天 **推播通知**（#44590）。  
- **歷史重載 UX**：背景重載時若有既有訊息或串流，**不收合 loading skeleton**；樂觀訊息在短暫空回應時 **保留**（#71844、#71878）；聊天歷史上限依 **可見訊息**（heartbeat／控制列過濾後）計算。  
- **Android**：**Talk Mode** 於 Voice 分頁露出；**voice capture modes**、mic **foreground service** 升級（見 changelog）。

### Control UI／Talk（2026.4.26）

- **瀏覽器 realtime**：通用 transport 合約；**Google Live**（受限 ephemeral token）；Gateway **relay** 支援後端 realtime 插件（詳見 CHANGELOG Fixes：維持 WebSocket、relay 上限等）。  
- **原始設定 diff**：側欄 **pending changes**，**JSON5** 解析、敏感欄 **redact** 至手動揭露（#39831）。

### Control UI／iOS／Meet（2026.4.27）

- **Dashboard**：頂欄／breadcrumb 顯示 **作用中 agent**（不含 session key）。  
- **Cron**：**新建工作**側欄 **可摺疊**。  
- **效能**：Debug／Overview **長任務／長影格**事件（可選）；並 **上限**條目以免擠掉其他診斷。  
- **Talk**：失敗橫幅 **可關閉**、清 **stale** 狀態、下次點擊 **直接重試**；OpenAI Realtime **CORS** 僅轉發允許標頭（#76435）。  
- **WebChat／媒體**：助理圖片採 **短期 ticket URL**（#70830、#77097）；**重複送出**折疊至同一 Gateway run（#75737）。  
- **iOS**：非 loopback **`ws://`** **拒絕**於核發 QR／setup code 前；Gateway 設定可 **掃 QR 或貼完整 setup code**。

### Android（2026.4.23）

- **Gateway 明文限制**：手動／掃描連線 **`ws://` 僅限 loopback**；私網／link-local 預設關閉（除非 TLS）
- **ASK_OPENCLAW**：外部 intent **只預填草稿**，不自動送出

### Diagnostics（2026.4.22）

- Gateway **預設**無 payload 穩定性記錄；**支援用診斷匯出**（脫敏日誌、狀態、健康、配置、穩定性快照）

### Voice Call（2026.4.23）

- OpenAI：**session 設定完成後**才問候／轉發緩衝音訊
- Twilio：**非 allowlist 主叫**在 stream 建立前拒絕

### Voice Call／Talk／Meet（2026.4.24）

- 即時語音路徑可 consult **完整 OpenClaw agent**（含工具編排），見 CHANGELOG Highlights 與 `06-通訊頻道` Google Meet／Voice 說明。

### Control UI／macOS／Talk（2026.5.2 精選）

- **Sessions／Cron**：畸形 cron **忽略**；預設列表 **有界**；**`chatMessageMaxWidth`** 設定鍵（#76051 等）。  
- **WebSocket／slash**：長連線 **protocol ping**；Stop **重連後仍可用**；iOS PWA **safe-area**；選取 **高對比**；本地 slash **錯誤回饋**（#70991、#60850、#52105）。  
- **Usage Mosaic**：UTC **15 分鐘 buckets**／hour filter（#74337）。  
- **Talk**：Realtime **CSP**、瀏覽器 **VAD／transcription** 設定、錯誤事件 surfaced（#73427）。  
- **macOS menu**：近期 session context → **Context 子選單**（用量／成本維持根層）；**Voice Wake／Talk／PTT** **downmix** 多聲道 mic（#42533）；WebChat **訂閱 transcript** 顯示使用者語音回合（#75155）；Tailscale **General** 開啟不誤寫 **`openclaw.json`**（#59545）。