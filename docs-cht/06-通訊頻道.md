# 通訊頻道

## 概述

OpenClaw 支援超過 30 種訊息平台，讓使用者能透過熟悉的聊天介面與 AI 互動。頻道分為內建頻道和擴充頻道兩類。

## 頻道架構

```
                    ┌─────────────────────────────────────┐
                    │          Channel Router             │
                    │                                     │
 WhatsApp ─────────▶│  ┌───────────────────────────────┐  │
 Telegram ─────────▶│  │     Inbound Handler           │  │
 Discord ──────────▶│  │  - 訊息解析                    │  │
 Slack ────────────▶│  │  - 路由檢查                    │  │
 Signal ───────────▶│  │  - allowlist 驗證              │  │
 iMessage ─────────▶│  └───────────────────────────────┘  │
 LINE ─────────────▶│              │                      │
 Google Chat ──────▶│              ▼                      │
                    │  ┌────────────────────────────────┐  │
 (Extensions)       │  │     Message Queue              │  │
 Matrix ───────────▶│  │  - 排隊處理                    │  │
 MS Teams ─────────▶│  │  - 併發控制                    │  │
 Zalo ─────────────▶│  └───────────────────────────────┘  │
 Feishu ───────────▶│              │                      │
 Synology Chat ────▶│              ▼                      │
                    │  ┌───────────────────────────────┐  │
                    │  │     Agent Processor           │  │◀─── Gateway
                    │  │  - LLM 呼叫                    │ │
                    │  │  - 工具執行                    │  │
                    │  └───────────────────────────────┘  │
                    │              │                      │
                    │              ▼                      │
                    │  ┌──────────────────────────────┐   │
                    │  │     Outbound Handler         │   │
                    │  │  - 格式轉換                   │   │
                    │  │  - 發送訊息                   │   │
                    │  └──────────────────────────────┘   │
                    │                                     │
                    └─────────────────────────────────────┘
```

## 內建頻道

### WhatsApp (Baileys)

**技術**: Baileys（WhatsApp Web 協議）

**特色**:

- 掃描 QR Code 登入
- 支援私訊和群組
- 媒體訊息（圖片、音訊、文件）
- 即時狀態（輸入中、已讀）

**配置**:

```json5
{
  channels: {
    whatsapp: {
      enabled: true,
      allowlist: ["+1234567890"],
    },
  },
}
```

**程式碼位置**: `src/web/`（含 `inbound/`, `auto-reply/` 子目錄）

### Telegram

**技術**: grammY（Telegram Bot API）

**特色**:

- Bot API（需要 Bot Token）
- 支援私訊和群組
- 豐富的訊息格式（Markdown、按鈕）
- Webhook 或 Polling 模式
- **串流預設 `partial`**（新安裝預設開啟即時預覽串流）
- **DM 私聊串流**（使用 `sendMessageDraft`，reasoning/answer 通道分離）
- **`disableAudioPreflight`**：跳過群組/topic 入站語音訊息的提及偵測前飛轉錄
- **Per-topic agentId 路由**：forum groups 和 DM topics 可設定個別 `agentId` 覆寫，路由到專屬 agent 並隔離 session
- Per-DM topic 配置（`direct` + topic allowlists、`dmPolicy`、`skills`、`systemPrompt`、`requireTopic`）
- DM topic 會話隔離（`<chatId>:<threadId>` scoped session key）
- 多帳號群組隔離（groups config 不再跨帳號洩漏）
- 多帳號預設路由：2+ 帳號未設 default 時僅發出警告，`openclaw doctor` 顯示缺失/無效預設帳號
- Plugin outbound hooks：Telegram 回覆路徑執行 `message_sending` + `message_sent` hooks
- 回覆媒體上下文（replied media files in inbound context）
- 語音 caption overflow 自動降級
- `replyToMode: "first"` chunking 支援
- 群組 allowlist 順序修正（chat allowlist 優先於 sender allowlist）
- 長模型按鈕 compact callback fallback（防止 Telegram 64 bytes callback 截斷）
- forum system messages 不觸發隱式提及偵測
- 保留入站媒體原始 `file_name` 元資料
- **Exec 審核**：OpenCode/Codex 執行審核支援
- **預覽超時防重複**：預覽編輯超時時防止重複訊息
- **Topic 指令路由**：原生 topic commands 路由至 active session
- **Slash SessionKey 命名空間**：按 agent 命名空間化
- **Webhook 健康恢復**：重啟後恢復 webhook 模式健康
- **下載超時**：防止 polling loop 掛起
- **停機中止**：停機時中止 getUpdates fetch
- **DM 去重**：每 agent 去重入站 DM 回覆
- **409 衝突重設**：webhook 清理鎖存器重設
- **Direct delivery hooks**：橋接至 internal message:sent hooks
- **媒體下載 IPv4 fallback**（2026.3.13+）：SSRF-guarded 檔案下載重試，IPv6 故障主機支援
- **Webhook secret 驗證**（2026.3.13+）：讀取 body 前先驗證 secret
- **媒體 URL 遮蔽**（2026.3.13+）：失敗下載時遮蔽 bot token
- **長 HTML 訊息分塊**（2026.3.13+）：分塊長 HTML 出站訊息
- **重複回覆預防**（2026.3.13+）：慢 LLM provider 時防止重複訊息
- **DM 主題自動重命名**（2026.3.23+）：首次訊息時自動重命名 DM 主題
- **自訂 apiRoot**（2026.3.23+）：支援替代 Telegram API 端點（如自架 Bot API Server）
- **可配置靜默錯誤回覆**（2026.3.23+）：錯誤回覆可配置為靜默模式
- **回覆上下文文字**（2026.3.23+）：保留 reply context text
- **Topic announce 交付**（2026.3.23+）：恢復 topic announce delivery
- **按鈕選填**（2026.3.23+）：message-tool buttons schema 改為 optional
- **asDocument 別名**（2026.3.23+）：記錄 asDocument 別名
- **Debounce 排序修復**（2026.3.23+）：修復 fire-and-forget 和 inbound debounce 排序
- **Thread create topic**（2026.3.24+）：thread create 使用 topic payload
- **無效照片預檢**（2026.3.24+）：預檢無效的 telegram photos
- **Forum topic 路由**（2026.3.24+）：保留 forum topic last-route delivery
- **Native commands snapshot**（2026.3.24+）：保持 native commands 在 runtime snapshot
- **Pairing code blocks**（2026.3.24+）：範圍限定 pairing code blocks
- **403 membership errors**（2026.3.24+）：改善 403 membership delivery 錯誤
- **Reaction ownership 持久化**（2026.4.5）：跨重啟保留 reaction ownership
- **Explicit topic targets**（2026.4.5）：回覆時保留明確 topic 目標
- **Media placeholder**（2026.4.5）：下載失敗時顯示 media placeholder 和 file_id
- **HTML formatting**（2026.4.5）：model switch 訊息啟用 HTML 格式
- **Native progress placeholder**（2026.4.5）：plugin 命令原生進度佔位符 opt-in
- **Private-network media opt-in**（2026.4.5）：危險私網媒體需明確 opt-in
- **Local Bot API trust**（2026.4.5）：信任 local bot API media roots
- **Paginated commands**（2026.4.5）：強制 callbacks 分頁命令
- **Error suppression controls**（2026.4.5）：錯誤抑制控制
- **Full provider/model comparison**（2026.4.5）：models picker 完整比較
- **Reasoning preview gating**（2026.4.5）：限制 reasoning previews 到 stream sessions
- **Forum Topic 名稱**（2026.4.14）：從 forum service messages 學習人類可讀的 topic 名稱，在 agent context、prompt metadata 和 plugin hook metadata 中展示
- **Topic 名稱持久化快取**（2026.4.14）：將已學習的 topic 名稱持久化到 Telegram session sidecar store，重啟後無需重新學習
- **全面重試機制**（2026.4.14）：approval callbacks、model callbacks、pagination callbacks、reaction updates、group migration updates 等全面支援重試；阻止 watermark 在失敗後前進
- **Status 指令解封**（2026.4.14）：修復忙碌 turns 期間 status 指令被阻塞的問題
- **Proxy DNS 媒體下載**（2026.4.14）：信任操作員配置的明確 proxy 進行媒體下載的 DNS 解析

**配置**:

```json5
{
  channels: {
    telegram: {
      enabled: true,
      token: "BOT_TOKEN",
      allowlist: ["@username", "123456789"],
    },
  },
}
```

**程式碼位置**: `src/telegram/`

### Discord

**技術**: Carbon

**特色**:

- Bot API（需要 Bot Token）
- 支援 DM 和伺服器頻道
- 斜線命令
- 訊息附件
- **`allowBots: "mentions"`**：僅接受提及機器人的 bot 訊息
- 串流預覽配置（`channels.discord.streaming`）
- Thread 綁定生命週期管理（`idleHours`/`maxAgeHours` 取代固定 TTL）
- `/session idle` 和 `/session max-age` 指令
- Components v2 支援（`cid` payload）
- Forum thread 標籤（`appliedTags`）
- 每帳號 `ackReactionScope` 覆寫
- 多帳號 `eventQueue.listenerTimeout` 配置
- Thread session 重置：thread 封存時重置 session，重新開啟時從新 session 開始
- 預設線上狀態：無自訂 presence 時自動發送 online presence
- Mention 處理：id-based 格式化 + cache 改寫、`ignoreOtherMentions` 設定
- Media SSRF allowlist：允許 Discord CDN 主機名（含萬用字元）
- Slash 命令路由：攔截文字 slash 命令、註冊外掛命令為原生、空 slash 執行 fallback
- Voice 解碼降級：使用 opusscript 取代原生 Opus 依賴
- Auto presence 健康信號：執行時可用性驅動的 presence 更新 + 連線狀態報告
- Agent media roots：`mediaLocalRoots` 透過 Discord reply delivery 傳遞
- **maxLinesPerMessage**：即時回覆中套用有效值
- **outbound mediaMaxMb**：遵從上傳大小限制
- **Guild slash auth**：遵從 `commands.allowFrom`
- **agentComponents 驗證**：驗證 config schema parity
- **Deploy rate limit 啟動**（2026.3.13+）：部署 rate limit 不阻塞啟動
- **Guild allowlist 強化**（2026.3.13+）：hydrated guild 物件缺失時使用 raw guild_id
- **autoArchiveDuration**（2026.3.13+）：新增自動封存時長配置
- **Reaction ingress**（2026.3.13+）：強制 users/roles allowlist 在 reaction ingress
- **非阻塞監聽器**：message listener 改為非阻塞
- **Native command DM allowlist**（2026.4.5）：尊重 allowlisted channels
- **Component-only media**（2026.4.5）：保留 component-only media 行為
- **@everyone mention gating**（2026.4.5）：檢測和門控 @everyone 提及
- **Default media cap 提升**（2026.4.5）：提升預設媒體上限
- **Thread starter resolution**（2026.4.5）：提取 forwarded message text
- **Voice timeout 拆分**（2026.4.5）：拆分語音超時
- **Proxy Carbon REST/webhook**（2026.4.5）：代理 Carbon REST、webhook 和 monitor fetch
- **Carbon ratelimit signature drift**（2026.4.5）：支援 Carbon ratelimit signature drift
- **Explicit reply tags**（2026.4.5）：投遞時尊重明確回覆標籤
- **原生 Status 回覆**（2026.4.14）：native `/status` 互動正確回傳狀態卡而非通用 ack
- **入站重試標準化**（2026.4.14）：入站重試改為明確行為，統一 replay dedupe

**配置**:

```json5
{
  channels: {
    discord: {
      enabled: true,
      token: "BOT_TOKEN",
      allowlist: ["user_id", "server_id"],
    },
  },
}
```

**程式碼位置**: `src/discord/`

### Slack

**技術**: @slack/bolt

**特色**:

- Slack App（需要 OAuth Token）
- 支援 DM 和頻道
- 互動式元件
- Webhook 模式
- **`typingReaction`**：Socket Mode DM 可透過反應表情顯示處理狀態（Slack 原生 assistant typing 不可用時的替代方案）
- **`replyToMode=off` session 路由**：top-level 頻道訊息共享單一 session
- **inbound debounce 路由**：按 message timestamp 隔離 debounce key，防止 cross-thread 衝突
- **thread context 優化**：現有 session 跳過 thread 起始/歷史文字注入，減少 token 浪費
- **Bolt 4.6 相容**：移除 `message.channels`/`message.groups` 事件處理器，改用 `channel_type` 統一 handler
- **mrkdwn 正規化**：串流和預覽路徑進行 mrkdwn 轉換
- **app_mention 重試 key**：dedup 檢查前記錄 retry key
- **系統事件路由**：路由至綁定 agent sessions
- **Dedup 恢復**：保留 dedup 同時恢復 dropped app_mention
- **mediaLocalRoots 傳播**：透過 send path 傳播
- **Reactions channel ID**：入站 context 中傳遞 channel ID
- **mrkdwn 重複轉換**：移除 native streaming path 的重複轉換
- **Stale socket 清除**：斷線後清除 stale socket state
- **allowlist scoping**：auto-reply allowlist store 按帳號 scope
- **Block Kit 訊息**（2026.3.13+）：支援 `channelData.slack.blocks` Block Kit 訊息
- **互動式回覆**（2026.3.13+）：opt-in 按鈕和選擇器回覆（`channels.slack.capabilities.interactiveReplies`）
- **Probe 簡化**（2026.3.13+）：auth.test() bot/team metadata 穩定
- **Thread context allowlist 過濾**（2026.4.5）：過濾 thread context 依 allowlist
- **Scoped prompts**（2026.4.5）：scoped prompts 和 mrkdwn hints
- **Live DM routing**（2026.4.5）：live DM replies 路由到 channel
- **Pre-set shuttingDown**（2026.4.5）：防止孤立 ping intervals
- **Groups:history scope**（2026.4.5）：app manifest 新增 groups:history scope
- **Typing status reaction**（2026.4.5）：typing status indicator 移至 reaction fallback

**配置**:

```json5
{
  channels: {
    slack: {
      enabled: true,
      token: "xoxb-...",
      signingSecret: "...",
      appToken: "xapp-...",
    },
  },
}
```

**程式碼位置**: `src/slack/`（含 `http/` 子目錄）

### Signal

**技術**: Signal 協議

**特色**:

- 端對端加密
- 私訊支援
- 需要 Signal CLI 或 signal-cli-rest-api
- **Groups 配置**（2026.3.13+）：per-group `requireMention`、`tools`、`toolsBySender` overrides 支援

**配置**:

```json5
{
  channels: {
    signal: {
      enabled: true,
      number: "+1234567890",
    },
  },
}
```

**程式碼位置**: `src/signal/`

### iMessage

**技術**: imsg CLI（macOS 限定）

**特色**:

- macOS 原生整合
- 私訊和群組
- 需要 Full Disk Access

**配置**:

```json5
{
  channels: {
    imessage: {
      enabled: true,
      allowlist: ["+1234567890", "user@icloud.com"],
    },
  },
}
```

**程式碼位置**: `src/imessage/`

### LINE

**技術**: LINE Messaging API

**特色**:

- LINE Messaging API
- DM/群組/聊天室支援
- Webhook Auth-Before-Body 安全模式
- 嚴格 auth 邊界語意（配對 store、DM/群組 allowlist 分離、fail-closed）
- 重播/重複控制（in-flight reservation + post-success dedupe）
- 檔案媒體下載修復 + M4A 音訊分類
- 群組/聊天室 peer 路由 + 命令授權上下文傳播
- Status 探測快取（snapshot/config state）
- Webhook HEAD 探測相容

**程式碼位置**: `src/line/`

## 擴充頻道（43 個擴充套件）

### 頻道擴充套件

| 頻道               | 說明                                                       | 位置                         |
| ------------------ | ---------------------------------------------------------- | ---------------------------- |
| Matrix             | 開放協議、端對端加密、多伺服器聯邦                         | `extensions/matrix/`         |
| Microsoft Teams    | 企業整合、Bot Framework                                    | `extensions/msteams/`        |
| Zalo               | 越南市場、OA 官方帳號                                      | `extensions/zalo/`           |
| Zalo Personal      | Zalo 個人帳號                                              | `extensions/zalouser/`       |
| LINE               | 日本/台灣市場                                              | `extensions/line/`           |
| 飛書 (Feishu/Lark) | 飛書 Bot API、Docx 表格/上傳、reactions、chat 工具、多帳號 | `extensions/feishu/`         |
| Google Chat        | Google Workspace                                           | `extensions/googlechat/`     |
| BlueBubbles        | iMessage 橋接                                              | `extensions/bluebubbles/`    |
| Mattermost         | 開源團隊協作（互動式按鈕 + 模型選擇器 + replyToMode 支援） | `extensions/mattermost/`     |
| Nextcloud Talk     | Nextcloud 整合                                             | `extensions/nextcloud-talk/` |
| Twitch             | 直播聊天                                                   | `extensions/twitch/`         |
| Nostr              | 去中心化協議                                               | `extensions/nostr/`          |
| IRC                | IRC 協議                                                   | `extensions/irc/`            |
| Tlon (Urbit)       | Urbit 網路                                                 | `extensions/tlon/`           |
| QQ Bot             | QQ 機器人（多帳號、SecretRef、斜線指令、媒體）             | `extensions/qqbot/`          |
| Synology Chat      | Synology NAS 聊天（含設定精靈）                            | `extensions/synology-chat/`  |
| Discord (擴充)     | Discord 擴充功能                                           | `extensions/discord/`        |
| Signal (擴充)      | Signal 擴充功能                                            | `extensions/signal/`         |
| Slack (擴充)       | Slack 擴充功能                                             | `extensions/slack/`          |
| Telegram (擴充)    | Telegram 擴充功能                                          | `extensions/telegram/`       |
| WhatsApp (擴充)    | WhatsApp 擴充功能                                          | `extensions/whatsapp/`       |
| iMessage (擴充)    | iMessage 擴充功能                                          | `extensions/imessage/`       |

### 功能擴充套件

| 擴充套件    | 說明                                          | 位置                      |
| ----------- | --------------------------------------------- | ------------------------- |
| voice-call  | 語音通話（Twilio, Telnyx, Plivo）             | `extensions/voice-call/`  |
| talk-voice  | 語音對話                                      | `extensions/talk-voice/`  |
| memory-core | 記憶體核心                                    | `extensions/memory-core/` |
| open-prose  | Prose 技能系統                                | `extensions/open-prose/`  |
| diffs       | 差異渲染工具（before/after 或 unified patch） | `extensions/diffs/`       |
| acpx        | ACPX 串流支援（pinned to 0.1.16）             | `extensions/acpx/`        |
| llm-task    | LLM 任務外掛（含 thinking override）         | `extensions/llm-task/`    |
| lobster     | Lobster 外掛                                  | `extensions/lobster/`     |
| chutes      | Chutes 計算擴充                               | `extensions/chutes/`      |
| firecrawl   | Firecrawl onboarding search plugin            | `extensions/firecrawl/`   |
| xai         | xAI (Grok) provider（含 fast mode）           | `extensions/xai/`         |
| deepseek    | DeepSeek bundled plugin                       | `extensions/deepseek/`    |

### Web Search 擴充套件

| 擴充套件    | 說明                                          | 位置                      |
| ----------- | --------------------------------------------- | ------------------------- |
| brave-web-search | Brave Search（預設啟用）                 | `extensions/brave-web-search/` |
| duckduckgo  | DuckDuckGo Search（experimental，免費）       | `extensions/duckduckgo/`  |
| exa         | Exa 語意搜尋 + extract                        | `extensions/exa/`         |
| tavily      | Tavily search + extract 雙工具                | `extensions/tavily/`      |

### 認證擴充套件

| 擴充套件                | 說明                    | 位置                                  |
| ----------------------- | ----------------------- | ------------------------------------- |
| google-antigravity-auth | Google Antigravity 認證 | `extensions/google-antigravity-auth/` |
| google-gemini-cli-auth  | Google Gemini CLI 認證  | `extensions/google-gemini-cli-auth/`  |
| minimax-portal-auth     | MiniMax Portal 認證     | `extensions/minimax-portal-auth/`     |
| qwen-portal-auth        | Qwen Portal 認證        | `extensions/qwen-portal-auth/`        |

## 頻道通用介面

### Channel 插件結構

```typescript
export const channelPlugin: ChannelPlugin = {
  id: "my-channel",
  name: "My Channel",

  async initialize(config: ChannelConfig) {
    /* 建立連接 */
  },
  async startMonitor(handler: MessageHandler) {
    /* 監聽訊息 */
  },
  async sendMessage(chatId: string, message: OutgoingMessage) {
    /* 發送 */
  },
  async getStatus(): Promise<ChannelStatus> {
    return { connected: true };
  },
  async shutdown() {
    /* 清理資源 */
  },
};
```

### 訊息格式

```typescript
interface IncomingMessage {
  id: string;
  channelId: string;
  chatId: string;
  senderId: string;
  senderName?: string;
  content: string;
  attachments?: Attachment[];
  replyTo?: string;
  timestamp: Date;
}

interface OutgoingMessage {
  content: string;
  format?: "text" | "markdown" | "html";
  attachments?: Attachment[];
  replyTo?: string;
  buttons?: Button[];
}
```

## 訊息路由

### Allowlist 機制

```json5
{
  channels: {
    telegram: {
      allowlist: ["@user1", "@user2", "group_id"],
    },
  },
}
```

Discord allowlist 名稱解析已規範化，確保一致的比對行為。Discord 新增 debug 日誌顯示 guild/channel allowlist 被丟棄的訊息。

### 多帳號預設路由

頻道支援可選的 `channels.<channel>.defaultAccount` 預設帳號選擇，當 `accountId` 未指定時路由到明確配置的帳號，而非依賴隱式排序。

### 串流預覽配置

統一的串流配置路徑：

```json5
{
  channels: {
    "<channel>": {
      streaming: {
        /* 串流預覽設定 */
      },
    },
  },
}
```

### DM 範圍

CLI 本地 onboarding 預設 `session.dmScope` 為 `per-channel-peer`。

## 頻道狀態

```bash
# 基本狀態
openclaw channels status

# 全部狀態（只讀/可貼上）
openclaw channels status --all

# 深度探測
openclaw channels status --deep

# 特定頻道
openclaw channels status telegram
```

## 頻道操作

```bash
# 連接 Telegram
openclaw channels connect telegram

# 連接 WhatsApp（會顯示 QR Code）
openclaw channels connect whatsapp
```

## 開發新頻道

### 1. 建立擴充套件目錄

```
extensions/my-channel/
├── openclaw.plugin.json
├── index.ts
├── package.json
└── src/
    ├── channel.ts
    ├── monitor.ts
    ├── send.ts
    └── runtime.ts
```

### 2. 定義插件元資料

```json
// openclaw.plugin.json
{
  "id": "my-channel",
  "channels": ["my-channel"],
  "configSchema": {
    "type": "object",
    "properties": {
      "apiKey": { "type": "string" },
      "allowlist": { "type": "array", "items": { "type": "string" } }
    },
    "required": ["apiKey"]
  }
}
```

### 3. 實作頻道插件

```typescript
// src/channel.ts
import type { ChannelPlugin, MessageHandler } from "openclaw/plugin-sdk";

export const channelPlugin: ChannelPlugin = {
  id: "my-channel",
  name: "My Channel",

  async initialize(config) {
    /* 初始化 SDK */
  },
  async startMonitor(handler: MessageHandler) {
    client.on("message", (msg) => {
      handler({
        id: msg.id,
        channelId: "my-channel",
        chatId: msg.chatId,
        senderId: msg.sender,
        content: msg.text,
        timestamp: new Date(),
      });
    });
  },
  async sendMessage(chatId, message) {
    await client.send(chatId, message.content);
  },
  async getStatus() {
    return { connected: client.isConnected() };
  },
};
```

### 4. 註冊插件

```typescript
// index.ts
import type { OpenClawPluginApi } from "openclaw/plugin-sdk";
import { channelPlugin } from "./src/channel.js";

const plugin = {
  id: "my-channel",
  name: "My Channel",
  register(api: OpenClawPluginApi) {
    api.registerChannel({ plugin: channelPlugin });
  },
};

export default plugin;
```

### 5. 更新標籤

新增頻道/擴充套件時，需更新 `.github/labeler.yml` 並建立對應的 GitHub 標籤。

## LINE 更新

- **requireMention gating**：完成 requireMention gating 行為

## Mattermost 更新

- **互動式按鈕**：新增 interactive buttons 支援
- **互動式模型選擇器**：新增 model picker
- **replyTo 參數**：plugin handleAction send 中讀取 replyTo
- **threaded replies**：payload.replyToId 作為 root_id
- **互動回調綁定**：強化 interaction callback binding
- **提及覆寫**：遵從 onmessage mention override

## WhatsApp 更新

- 入站 `fromMe` 上下文傳播，agent 可區分 owner 自發訊息（標記 `(self)`）
- 主 DM 路由改為單一 owner
- **outbound mediaMaxMb**：遵從上傳大小限制

## Signal 更新

- **入站附件轉發**：轉發所有入站附件

## iMessage 更新

- **Echo loop 修復**：防止 echo loop 洩漏內部 metadata 和 NO_REPLY 溢出

## Synology Chat

Synology Chat 整合功能：

- **設定精靈**（2026.3.23+）：新增引導式設定精靈
- **Webhook 相容性**：接受 JSON 和別名 payload 欄位，支援 body/query/header token 來源，以 `204` ACK 防止 "Processing..." 狀態
- **Body read 邊界**：強制 size + timeout bound，防止慢速 DoS
- **回覆交付**：解析 webhook username → Chat API `user_id`
- **啟動循環修復**：`startAccount` 等到 abort 防止 webhook 路由重啟循環
- **多帳號 webhook**（2026.3.23+）：釐清 multi-account webhook paths
- **危險名稱匹配**（2026.3.23+）：集中化和審計 dangerous name matching
- **Reply name gating**（2026.3.23+）：收緊 reply name matching
- **Fail closed webhooks**（2026.3.23+）：shared webhook paths fail closed
- **Config schema 去重**（2026.3.23+）：去重 config schema

## 飛書 (Feishu) 進階功能

### Docx 表格與上傳

```json5
{
  channels: {
    feishu: {
      tools: {
        doc: true, // feishu_doc 工具
        chat: true, // feishu_chat 工具
      },
    },
  },
}
```

- `feishu_doc` 操作：`create_table`、`write_table_cells`、`create_table_with_values`、`upload_image`、`upload_file`
- `feishu_chat` 操作：聊天資訊與成員查詢
- Reactions 處理：`im.message.reaction.created_v1` 事件
- 群組會話範圍：`group`、`group_sender`、`group_topic`、`group_topic_sender`
- `replyInThread` 配置（`disabled | enabled`）
- 多帳號 `defaultAccount` 路由
- `typingIndicator` 和 `resolveSenderNames` 可獨立停用
- `reactionNotifications`（`off | own | all`）
- 探針狀態快取（10 分鐘）
- **topic session 路由**：`thread_id` 作為 session scope fallback，topic key 首次穩定
- **topic 根回覆**：優先使用 `root_id` 作為 `replyTargetMessageId`
- **DM 配對回覆**：發送至 `chat:<chat_id>` 而非 `user:<open_id>`
- **Lark 私聊路由**：`chat_type: "private"` 視為 DM 上下文
- **訊息 debounce**：快速同聊天連發 debounce 成一次有序分發
- **訊息序列化**：每聊天序列化訊息處理，保持跨聊天並行
- **dedup 重啟持久化**：monitor 啟動時熱載入 dedup 狀態
- **stale 查詢抑制**：不顯示 `contact:contact.base:readonly` 非必要提示
- **非 ASCII 檔名編碼**：Feishu 多部分上傳中正確 percent-encode 中文/特殊字元檔名
- **本地圖片轉換**：sendText 中傳遞 mediaLocalRoots
- **Block streaming 修復**：停用 block streaming 防止靜默回覆丟失
- **SDK 超時修復**：移除無效的 timeout 屬性
- **完整回覆機制**：outbound replyToId 轉發 + topic-aware reply targeting
- **MP4 影片類型**：使用 msg_type media 發送 MP4
- **全域 HTTP 超時修復**：避免媒體回歸
- **Streaming 合併語義**：強化 streaming merge 和最終回覆去重

## 最佳實踐

1. **錯誤處理**: 妥善處理網路錯誤和 API 限制
2. **重連機制**: 實作自動重連和指數退避
3. **速率限制**: 遵守平台 API 限制
4. **日誌記錄**: 記錄關鍵操作便於除錯
5. **安全性**: 妥善保管認證憑證
6. **串流**: 只向最終回覆傳送到外部頻道，不傳送串流/部分回覆
7. **TTS 語音氣泡**: 支援 Feishu/WhatsApp/Telegram/Matrix 的語音氣泡播放（opus 輸出）

## Discord 更新（2026.3.23+）

- **原生指令 auth 失敗**：在 auth 失敗時回覆
- **訊息工具發送**：解除 Discord message tool 發送阻塞
- **圖片交付**：修復 generated image 交付至 Discord
- **autoThreadName 'generated'**（2026.3.24+）：LLM 自動生成 thread 名稱
- **/think autocomplete**（2026.3.24+）：從 session model 解析 /think autocomplete
- **Safety error listener**（2026.3.24+）：teardown 後移除防止 memory leak
- **Gateway crash 防護**（2026.3.24+）：防止未捕獲 errors 導致 crash
- **autoArchiveDuration 型別**（2026.3.24+）：新增至 config 型別
- **Gateway supervision**（2026.3.24+）：集中化 Discord gateway supervision

## Slack 更新（2026.3.23+）

- **交付鏡像重複防止**：防止 delivery-mirror 重複交付
- **Chunk 限制提升**：提升 Slack chunk 限制
- **訊息工具發送**：解除 Slack message tool 發送阻塞

## Matrix 更新（2026.3.23+）

- **Runtime API exports**：避免重複 runtime api exports
- **Send aliases**：保留 send aliases 和 voice intent
- **Voice bubble**：新增 matrix 至 VOICE_BUBBLE_CHANNELS
- **Room bindings**：避免觸碰 dropped room bindings

## Nostr 頻道（2026.3.23+）

新增 Nostr 去中心化協議頻道：
- **Setup wizard**：統一的頻道設定發現
- **Inbound DM policy**：解密前強制 inbound DM 政策
- **Chat plugin builder**：採用 chat plugin builder

## Outbound 架構遷移（2026.3.23+）

Outbound delivery 系統大量遷移至插件架構：
- Target resolution 和 display fallbacks 由 channel plugins 控制
- Session routing 透過 channel plugins
- Interactive payloads 共用化
- Delivery queue storage 和 recovery 分離
- Broadcast channel scan 在 channel 明確時跳過

## WhatsApp 更新（2026.3.24+）

- **FutureProofMessage 解包**：解包 botInvokeMessage 恢復 reply-to-bot 偵測
- **selfLid 比對**：使用 selfLid 進行群組 reply-to-bot 隱式提及
- **selfLid 讀取**：從 creds.json 讀取 selfLid
- **非同步檔案讀取**：使用 async fs.promises.readFile
- **Active listener**：恢復 active listener singleton
- **Identity handling**：統一 whatsapp identity handling

## Feishu 更新（2026.3.24+）

- **Inbound timestamps**：使用 message create_time 作為 inbound timestamps
- **WebSocket 關閉**：monitor stop 時關閉 WebSocket 連線
- **Open-group docs**：完成 open-group 文件和 baselines
- **requireMention 預設**：groupPolicy open 時預設為 false
- **Retry helper 打磨**：改善 retry helper 邏輯
- **群組訊息丟棄防護**：防止 bot-info probe 逾時時的靜默群組訊息丟棄

## Feishu 安全強化（2026.3.28+）

- **Card commands 拒絕**：拒絕 legacy raw card 指令 payload
- **Webhook 簽名驗證**：解析前驗證 webhook 簽名
- **LocalRoots sandbox**：docx 上傳強制 localRoots sandbox

## MS Teams 更新（2026.3.24+）

- **訊息編輯/刪除**：新增訊息編輯和刪除支援
- **結構化引用/回覆**：提取結構化 quote/reply 上下文
- **Entra JWT**（2026.3.28+）：接受 strict Bot Framework 和 Entra service tokens
- **Search message**（2026.3.28+）：新增搜尋訊息 action
- **Thread history**（2026.3.28+）：透過 Graph API 擷取 channel reply thread 歷史

## Telegram 更新（2026.3.28+）

- **Forum topic routing**：保留 /new 和 /reset 的 forum topic routing
- **Empty text skip**：跳過空文字回覆避免 GrammyError 400 crash
- **Word boundary split**：長訊息在字詞邊界分割
- **ReplyToMessageId 驗證**：發送前驗證 replyToMessageId
- **Model name display**：模型選擇器顯示名稱而非 ID
- **Self-authored DM**：忽略自己發送的 DM message updates

## Matrix 更新（2026.3.28+）

- **E2EE 縮圖加密**：在 E2EE rooms 中使用 thumbnail_file 加密縮圖
- **ESM __dirname**：使用 createRequire 修復 ESM 中的 __dirname
- **Plugin load crash**：防止 matrix-js-sdk plugin 載入 crash
- **Directory auth**：修復 directory auth 和 credentials fallback

## LINE 安全更新（2026.3.28+）

- **Timing-safe HMAC**：消除 HMAC 簽名驗證中的 timing side-channel
- **Status checks**：使用 configured field 進行 status checks

## 其他頻道更新（2026.3.28+）

| 頻道 | 改進 | PR |
|------|------|-----|
| Slack | 新增 upload-file action | #54987 |
| iMessage | 停止在 outbound text 中洩漏 reply tags | #39512 |
| WhatsApp | self-chat DM echo 過濾、quoted message 解包 | #54570 |
| BlueBubbles | 群組參與者 Contacts 名稱豐富、private network 自動允許 | #54984 |
| Synology Chat | webhook token 猜測限流 | #55141 |
| WeChat | 透過官方騰訊 iLink Bot plugin 新增 | #52131 |

## 頻道更新（2026.3.31）

### QQ Bot（全新頻道）

新增 QQ Bot 為 bundled channel plugin：
- 多帳號設定
- SecretRef-aware 憑證
- 斜線指令
- 提醒功能
- 媒體收發支援
- 語音 schema 驗證

**位置**: `extensions/qqbot/`

### Matrix（2026.3.31）

- **Draft Streaming**：部分回覆以就地編輯方式更新同一訊息，而非每個 chunk 發送新訊息（#56387）
- **Thread-Isolated Sessions**：per-DM `threadReplies` 覆寫，thread session 隔離對齊觸發訊息（#57995）
- **Group Chat History**：`channels.matrix.historyLimit` 群組觸發上下文，per-agent watermarks（#57022）
- **Proxy Config**：明確 `channels.matrix.proxy` HTTP(S) 代理配置（#56931）
- **DM 三層分類**：三層 `is_direct` 邏輯和 2-member guard（#57124）
- **Sender Allowlist 過濾**：fetched room context 按 sender allowlist 過濾（#58376）
- **DM Invite 提升**：收緊 DM invite promotion state（#58099）
- **顯示名稱 Mentions**：displayName labels 的 mentions 修正（#55393）
- **引用訊息上下文**：reply context body 和 sender 解析（#55056）
- **Crypto Bootstrap**：修復 crypto bootstrap 失敗和 multi-extension idHint warning（#53298）
- **SecretRef Fallback**：修復 env SecretRef fallback in clean()（#54980）
- **Outbound Direct-Room**：對齊 outbound direct-room selection（#56076）
- **2-member DM Fallback**：僅在 DM refresh 失敗時使用 2-member DM fallback（#54890）

### Telegram（2026.3.31）

- **Polling Watchdog**：防止 polling watchdog 掉回覆（#56343）
- **RFC2544 媒體下載**：允許 RFC2544 media downloads（#57624）
- **Audio Preflight Auth**：群組/topic 語音轉錄需發送者授權（#57566）
- **Stalled Polling 重建**：stalled polling cycles 後重建 transport
- **Pairing Allowlist 遷移**：遷移 pairing allowFrom 到 default account only（#58165）
- **Config 遷移**：遷移已移除的 `groupMentionsOnly` 配置（#55336）
- **Media SSRF Policy**：收緊 media SSRF policy（#56004）
- **DM Auth for Callbacks**：強制 DM auth for callbacks（#55112）
- **Sanitize Stream Errors**：消毒無效的 stream-order errors（#55999）

### Discord（2026.3.31）

- **Group DM 路由**：修復 Group DM component interaction routing 和 auth（#57763）
- **Native Commands Allowlist**：修復 native commands 繞過 group DM channel allowlist（#57735）
- **Voice Ingress Allowlist**：語音 ingress 需 member access（#58245）
- **Component Policy Gates**：元件互動政策閘門（#56014）
- **Approver Checks**：text approvals 的 approver 檢查（#56015）
- **Stale Socket Reconnect**：修復 stale-socket reconnect crash（#54697, #55991）
- **Carbon Beta**：更新 carbon beta

### Slack（2026.3.31）

- **原生 Exec Approvals**：exec approval 提示留在 Slack（#58155）
- **Status Reactions**：工具/thinking 進度 reaction 指示器（#56430）
- **Duplicate Draft Reply**：防止重複 draft reply
- **Interactive Block Delivery**：完成互動式 block delivery（#57473）
- **Table Block Mode**：恢復 table block mode seam（#57591）
- **Upload-File Action**：新增 upload-file action（#54987）

### LINE（2026.3.31）

- **外發媒體**：圖片、影片、音訊的外發傳送支援
- **ACP Binding**：ACP binding 功能同等性（#56700）
- **Markdown 底線保留**：保留 CJK/Latin 單詞內的底線（#47465）
- **Timing-Safe HMAC**：消除 HMAC 簽名驗證中的 timing side-channel（#55663）
- **Canonical ACP Targets**：正規化 canonical ACP targets（#56713）

### MS Teams（2026.3.31）

- **Graph API Member Info**：Graph-backed 成員資訊 action（#57528）
- **Thread History via Graph**：Graph API thread history 擷取（#51643）
- **搜尋訊息 Action**：搜尋訊息 action（#54832）
- **Progressive Delivery**：blockStreaming 配置（#56134）
- **Stream State Reset**：tool calls 後重置 stream state 防止訊息遺失（#56071）
- **Freshest DM Reference**：偏好最新的 personal conversation reference（#54702）
- **Webhook Auth Validation**：JSON parsing 前驗證 webhook auth（#57686）
- **Strict JWT**：接受 strict Bot Framework 和 Entra service tokens（#56631）
- **Feedback Invoke Auth**：對齊 feedback invoke authorization（#55108）

### WhatsApp（2026.3.31）

- **Emoji Reactions**：agent 可對 WhatsApp 訊息以 emoji reaction 回應
- **群組 Hydrate on Connect**：連接時 hydrate participating groups（#58007）
- **Self-Chat Echo 過濾**：使用 outbound ID tracking 過濾 self-chat DM echo（#54570）
- **Quoted Wrapper 解包**：解包 quoted wrapper messages
- **Quoted MentionedJids 排除**：排除 quoted message 的 mentionedJids（#52711）

### 其他頻道（2026.3.31）

| 頻道 | 改進 |
|------|------|
| Feishu | 群組 thread context 過濾（#58237）、webhook 簽名驗證（#55083）、card command 拒絕（#55130）、explicit empty settings 保留（#58370） |
| BlueBubbles | debounce flush null guard（#56573）、webhook auth 節流（#55133）、群組參與者名稱豐富（#54984）、private network 自動允許 |
| Nostr | DM 簽名驗證（#58236） |
| Mattermost | stale websocket 偵測（#53604）、reply config 傳遞（#48347）、stream state reset（#56071） |
| Synology Chat | webhook in-flight guard（#57722）、token guess 節流（#55141） |
| Nextcloud Talk | webhook auth 失敗節流（#56007） |
| Voice Call | Plivo callback origin 固定（#58238）、oversized frame 拒絕（#58241）、Telnyx replay key 正規化（#57829） |
| Google Chat | stable group ids 要求（#55131） |

### Matrix（2026.4.5）

- **原生 Exec Approvals**：Matrix 頻道新增原生 exec approval 支援（#58635），含 reaction 快捷鍵
- **Split Partial/Quiet Preview Streaming**：拆分部分和安靜預覽串流（#61450）
- **IDB Crypto Persistence**：advisory file locking 提升資料安全（#59851）
- **Secret Storage Recreation**：修復啟動時驗證失敗（#59846）
- **Room Allow Aliases 遷移**：遷移至 enabled（#60690）
- **Spec-Compliant Mentions**：發射符合規範的 mentions（#59323）
- **Media Size Limit**：超出 Matrix 媒體大小限制時顯示使用者可見標記（#60289）
- **Guided Setup Flow**：恢復引導式設定流程（#59462）
- **Canonical Private-Network Opt-In**：尊重 canonical private-network opt-in
- **Account Scoping**：account scoping 和 default detection 收緊
- **Pinned Dispatcher Recovery**：pinned dispatcher runtime 故障恢復（#61595）
- **Legacy SSSS Key 處理**：SSSS recreation on backup reset when SSSS key is broken（#60599）
- **Reasoning Preview Gating**：限制 reasoning previews 到 stream sessions

### Telegram（2026.4.5）

（見 Telegram 主段落的 2026.4.5 更新）

### Discord（2026.4.5）

（見 Discord 主段落的 2026.4.5 更新）

### Slack（2026.4.5）

（見 Slack 主段落的 2026.4.5 更新）

### MS Teams（2026.4.5）

- **DM Inline Images**：Graph API 下載 DM inline images（#52212）
- **Channel-List/Channel-Info Actions**：新增 channel-list 和 channel-info actions（#57529）
- **OpenClaw User-Agent Header**：Microsoft HTTP 呼叫加入 User-Agent（#51568）
- **Conversation Reference DM Pairing**：DM pairing 持久化 conversation reference（#60432）
- **Adaptive Card Action.Submit**：處理 Adaptive Card invoke activities（#60431）
- **TypingIndicator Config**：typingIndicator 配置和防止重複 DM typing（#60771）
- **HttpPlugin 替換**：使用 httpServerAdapter 替換 deprecated HttpPlugin（#60939）
- **Path-To-Regexp Express 5**：修正 Express 5 crash（#55440）
- **DM Threading Proactive Fallback**：保留 channel reply threading（#55198）

### WhatsApp（2026.4.5）

- **Reaction Guidance Levels**：可配置的 reaction 指導等級（#58622）
- **Block Streaming Config**：尊重 block streaming 配置（#60069）
- **Self-Chat Quoted Replies**：群組中忽略 self-chat quoted replies（#60148）
- **HTML/XML/CSS MIME Map**：新增 MIME map + 未知媒體 fallback（#51562）
- **SelfChatMode Presence**：gate connect-time presence on selfChatMode（#59410）
- **Watchdog Timeout Reset**：重連後重置 watchdog timeout（#60007）

### 其他頻道（2026.4.5）

| 頻道 | 改進 |
|------|------|
| Feishu | Comment event（#58497）、schema 2.0 card config（#53395）、reasoning preview gating（#61271） |
| Mattermost | Groups property config（#57618）、slash command token validation hardening |
| Line | Dist runtime contract path 修正、lazy-load runtime seams |
| QQ Bot | Silk-wasm lazy-load（#58829）、structured payload path restriction（#58453）、bot-logs allowlist（#58895） |
| Voice Call | STT connect timeout clear（#58586）、full config for realtime transcription（#61224） |
| IRC | Runtime API smoke coverage |
| QA Lab | 全新 QA 頻道和擴充（含 Docker stack、suite runner、debugger UI） |

## 頻道更新（2026.4.8）

### Slack（2026.4.8）

- **Proxy 完整支援**：Socket Mode WebSocket 連線遵從 `HTTPS_PROXY` / `NO_PROXY` 環境變數（#62878）
- **Download Token 修復**：正確傳遞已解析的 Slack download tokens（#62097）
- **Thread Explicit Mention**：新增 `thread.requireExplicitMention` 配置選項（#58276）
- **Streaming 文件釐清**：更新 Slack native streaming 在 thread 中的行為文件
- **Guarded Media Transport**：修復 Slack guarded media 傳輸（#62239）
- **Fallback Thread 分類**：保留 Slack fallback thread 分類（#61835）
- **Channel Streaming Config**：正規化 channel streaming config shape（#61381）

### Discord（2026.4.8）

- **Event Cover Image**：Discord 事件建立支援封面圖片（#60883）
- **Voice Recovery**：加強 Discord 語音接收恢復機制（#41536）
- **Voice Listener Compat**：新增 voice listener 相容墊片

### Telegram（2026.4.8）

- **Long Message Splitting**：恢復長訊息的出站分割（#57816）
- **Startup Timeout**：限制啟動請求超時（#61601）
- **Setup Package Fix**：修復 Telegram setup package entry

### Matrix（2026.4.8）

- **Auth Bootstrap**：強化 Matrix 啟動 auth bootstrap（#61383）
- **Health Probe deviceId**：health probe 傳遞 deviceId 防止 storage-meta 覆寫（#61581）
- **Auto-join Onboarding**：onboarding 中提示 invite auto-join（#62168）

### QA Lab（2026.4.8）

- **全新 UI**：Sidebar 佈局、Slack 風格聊天介面、Light/Dark 模式
- **一鍵 Docker**：`openclaw qa-lab` 一鍵啟動完整 Docker 環境
- **互動式 Suite Runner**：互動式測試套件執行器
- **Frontier Bakeoff**：前沿模型對比評估迴圈
- **Markdown 場景定義**：場景定義遷移至獨立 Markdown 檔案
- **Fast Refresh**：UI 快速刷新模式
- **10+ 新場景**：新增附件理解等測試場景

### 其他（2026.4.8）

| 頻道 | 改進 |
|------|------|
| Windows | Plugin loader alias 解析保留（#61832） |
| Allowlist | Write commands 在 channel 解析前需 owner 檢查（#62383） |

---

## 頻道改善（2026.4.12）

### 飛書 Feishu（2026.4.12）

- **QR Code Onboarding**：掃碼建立流程，自動建立 Feishu 應用並設定 webhook（#65680）
- **文件評論會話**：支援飛書文件評論作為會話來源（#63785）
- **UA 標準化**：統一飛書 HTTP 請求 User-Agent 標頭（#63835）
- **Auth Login 循環修復**：打破 auth login barrel 循環引用（feishu auth cycle fixes）
- **App Registration Guard**：保護 app registration fetch 避免未註冊時的錯誤（guard app registration fetches）

### MS Teams（2026.4.12）

- **Federated Credentials**：支援 certificate、managed identity、federated credential 認證（#53615）
- **Thread Context Injection**：對話上下文注入 thread context 以改善多輪對話（#54932）
- **SSO 支援**：signin/tokenExchange 單一登入流程支援（#60956）
- **Streaming Typing Indicator**：串流回覆時顯示打字指示器（#59731）

### Matrix（2026.4.12）

- **MSC4357 Live Streaming Markers**：支援 draft-stream 即時編輯串流標記（#63513）
- **原生 Exec Approvals**：Matrix 原生執行核准流程

### Discord（2026.4.12）

- **Heartbeat Timer 修復**：修復 heartbeat timer 計時問題（#65087）
- **Streaming Alias 標準化**：統一串流別名解析
- **Auto TTS Voice Notes**：自動 TTS 以語音訊息形式發送（#64096）

### Telegram（2026.4.12）

- **Approval Callback Deadlock 修復**：修復核准回調死鎖問題（#64979）

### Slack（2026.4.12）

- **Same-origin Media Auth**：同源媒體認證確保安全傳輸（#62996）
- **Streaming Dedupe**：串流回覆去重防止重複訊息（#62859）

### QQ Bot（2026.4.12）

- **SSRF URL List 修復**：修復 SSRF URL 白名單列表問題（#65788）

### iMessage（2026.4.12）

- **watch.subscribe Retry**：watch.subscribe 連線重試機制（#65482）

### WhatsApp（2026.4.12）

- **Media URL 修復**：修復媒體 URL 解析問題（#64394）

### QA Lab（2026.4.12）

- **GPT-5.4 Parity Proof**：前沿模型同等性驗證（GPT-5.4 parity proof）
- **Character Eval 系統**：角色評估系統，驗證 Agent 人格一致性
- **Multipass Runner**：多次執行測試運行器提升可信度
- **Suite 改進**：測試套件結構優化與新場景

---

## 各頻道改進歷史（2026.4.13–2026.4.14）

### Telegram（2026.4.13–14）

- **Forum Topic 名稱**：從 forum service messages 學習人類可讀的 topic 名稱，在 agent context、prompt metadata 和 plugin hook metadata 中展示（#65973）
- **Topic 名稱持久化**：將已學習的 topic 名稱儲存到 Telegram session sidecar store，重啟後無需重新學習（#66107）
- **全面重試機制**：approval callbacks、model callbacks、pagination callbacks、reaction updates、group migration updates 等全部支援重試
- **Watermark 管理**：阻止 watermark 在失敗後前進；延遲重播 commit 直到更新成功
- **Status 指令解封**：修復忙碌 turns 期間 status 指令被阻塞（#66226）
- **Proxy DNS 媒體**：信任操作員配置明確 proxy 進行媒體下載的 DNS 解析（#66461）

### Discord（2026.4.13–14）

- **原生 Status 回覆**：native `/status` 互動正確回傳狀態卡而非通用 ack（#54629）
- **入站重試標準化**：入站重試改為明確行為

### Slack（2026.4.13–14）

- **互動事件 Allowlist**：將全域 `allowFrom` allowlist 套用到 block-action 和 modal 互動事件（#66028）
- **Plugin Commands**：當頻道支援 native commands 時允許 plugin commands 在 Slack 運行（#64578）
- **Doctor Contract API 隔離**：Slack doctor contract API 獨立隔離（#63192）
- **入站重試標準化**：入站重試改為明確行為

### Feishu（飛書）（2026.4.13–14）

- **Allowlist Target 規範化**：加強 allowlist target 規範化（#66021）
- **Card Action / Bot Menu 重試**：card action 和 bot menu 操作改為明確重試
- **Comment Replay 修復**：generic failures 後保持 comment replay 關閉

### WhatsApp（2026.4.13–14）

- **Encrypted Write Stream**：等待 write stream 完成後才回傳 encFilePath，修復加密媒體文件早讀問題（#65896）
- **Baileys Plugin-Local**：Baileys 保持為 plugin-local 依賴
- **入站重試標準化**：入站重試改為明確行為

### Voice Call（2026.4.13–14）

- **重試機制強化**：retryable errors、rejected inbound hangups、unknown-call replays 全部支援重試

### Matrix / Mattermost / LINE / Zalo / Nextcloud Talk / TLON / Nostr（2026.4.13–14）

- **跨頻道 Replay Dedupe 標準化**：統一所有頻道的重播去重複、可重試失敗釋放和成功後提交行為
- Mattermost dedupe 重複 model picker 選擇
- Nostr：去重確定性被拒絕事件，失敗後重試入站事件
- TLON：失敗後釋放 replay claims

### QA Lab（2026.4.13–14）

- **Broken Turn Recovery**：新增損壞 turn 恢復場景（#66416）
- **GPT-Style Broken Turns**：GPT 風格損壞 turn 測試覆蓋

## 各頻道改進歷史（2026.4.15）

### Telegram（2026.4.15）

- **二進制 Payload 消毒**：丟棄入站 Telegram 文字中洩漏的二進制 caption bytes（`.mobi`、`.epub`），防止 prompt token 爆炸（#66877, #66663）
- **命令同步本地化**：保持 Telegram 命令同步快取 process-local，gateway 重啟後重新註冊選單而非信任過期磁碟狀態（#66730）
- **原生 Auto 預設恢復**：恢復 native commands 和 native skills 的插件 registry-backed auto 預設，slash commands 保持正確註冊（#66843）

### Discord（2026.4.15）

- **Function Tag 去除**：去除 assistant 文字中獨立的 Gemma 風格 `<function>...</function>` tool-call payloads，不截斷散文（#67318）
- **Carbon Slow Listener**：提高 carbon slow listener 閾值，減少假警告

### Slack（2026.4.15）

- **CSV/Markdown 上傳**：允許主機本地 CSV 和 Markdown 上傳（確認為真正文字時）（#67047）
- **Slash Commands Button 修復**：修復 `/verbose` 等 slash commands 使用 native buttons 時 option menu 錯誤（#67082）

### BlueBubbles（2026.4.15）

- **入站圖片修復**：在 Node 22+ 恢復入站圖片附件下載，接受 `updated-message` webhooks，修復 event-type-aware 去重複（#67510）
- **Catchup 重試上限**：新增 `catchup.maxFailureRetries`（預設 10），格式錯誤訊息不再永遠阻塞 cursor（#67426）
- **Restart 後 Replay**：透過持久化 per-account cursor 和 `/api/v1/message/query?after=<ts>` 重播遺漏訊息（#66857）
- **跨重啟去重複**：持久化文件支援的 GUID dedupe，防止 BB Server 重啟後重複回覆（#66816）

### WhatsApp（2026.4.15）

- **Creds Queue 排空**：重新開啟 socket 前先排空待處理的 creds save queue，防止 creds.json 寫入競態（#67464）
- **原子寫入 creds.json**：creds.json 改為原子寫入（#63577）
- **Baileys Media Upload 強化**：強化加密上傳處理，避免大型媒體發送的 buffer 峰值（#65966）

### Feishu（2026.4.15）

- **Webhook 強化**：缺少 `encryptKey` 時 fail closed；拒絕無 key 時的未簽名請求；丟棄空白 card-action token（#66707）

### Matrix（2026.4.15）

- **DM 配對安全**：阻止 DM pairing-store 條目授權 room control commands（#67294）
- **Pairing-Store 讀取跳過**：跳過 room 流量的 DM pairing-store 讀取（#67325）
- **E2EE SSSS Bootstrap**：修復 passwordless token-auth bots 的 E2EE SSSS bootstrap，不再需要 password（#66228）
- **Media Alias 正規化**：修復 Matrix media alias 正規化

### Voice / Audio（2026.4.15）

- **自架 STT 恢復**：恢復 `models.providers.*.request.allowPrivateNetwork` 用於音訊轉錄（#66692）

### Speech / TTS（2026.4.15）

- **TTS Provider 自動啟用**：自動啟用 Microsoft 和 ElevenLabs speech providers（#62846）
- **Google Gemini TTS**：新增 Google Gemini TTS provider，支援 WAV 和 PCM telephony 輸出（#67515）
- **TTS 路由修復**：透過明確或活動 provider 路由 `[[tts:speed=1.2]]` 覆寫，修復落在錯誤 provider 的問題

### Cron / Announce（2026.4.15）

- **NO_REPLY 抑制強化**：保持 cron announce NO_REPLY 剝離大小寫不敏感（#65016）
- **Announce 全欄位保留**：移除 announce delivery 中的摘要指令，保留所有欄位（#65638）

### QA Matrix（2026.4.15）

- **Matrix QA 拆分**：Matrix live QA 拆分為獨立的 `qa-matrix` runner，repo-private 表面不在打包版本中（#66723）
- **E2EE 驗證 Hooks**：暴露 E2EE QA verification hooks
- **大量 Matrix 覆蓋**：完成 Matrix P1 harness 覆蓋，新增 transport substrate 和 contract coverage

## 各頻道改進歷史（2026.4.18–2026.4.20）

### Telegram（2026.4.18–20）

- **Polling Transport 有界連接池（重要！）**：修復長期運行 gateway 累積大量 `api.telegram.org` 連接的問題；transports 現在有正確的 `close()` lifecycle 和嚴格的 per-origin pool cap
- **Polling Stall 閾值提升**：預設閾值從 90s 提升至 120s，可透過 `channels.telegram.pollingStallThresholdMs` 配置
- **Polling Offset Timeout**：Offset confirmation `getUpdates` probe 加入 client-side timeout，防止 zombie socket 阻塞 polling 恢復
- **Streaming 修復系列**：auto-compaction 時保持 transient preview；修復 abort 後重播舊回覆；清除 compaction replay guard
- **Callback 修復**：永久 callback edit 錯誤視為已完成，修復 stale command pagination buttons
- **ACP Bindings 清理**：重啟時清理指向缺失或失敗 ACP sessions 的持久化 DM bindings
- **Status Reactions**：遵守 `messages.removeAckAfterReply` 設定

### Discord（2026.4.18–20）

- **Think Autocomplete**：`/think` 只對支援 adaptive thinking 的 model 顯示 `adaptive`，GPT 模型不顯示 Anthropic-only 選項
- **Slash Commands 部分元資料**：容忍部分 Discord channel 元資料，不再 crash

### Slack（2026.4.18–20）

- **SecretRef Token 修復**：修復透過 `file` 或 `exec` secret sources 配置帳戶的 outbound replies 失敗問題
- **Native Streaming 收件方**：從 inbound user 解析 native streaming recipient team
- **Thread Context**：Thread starter 和 history fetches 失敗記錄在 verbose level，保持 best-effort fallback

### WhatsApp（2026.4.18–20）

- **Multi-account Inbound Policy 集中管理**：named-account inbound policy、per-account group activation 和 session keys 隔離
- **Auth Persistence 強化**：WhatsApp auth persistence 和 backup recovery 強化；backup-backed login 不強制 QR
- **Setup 驗證**：guard personal-phone 和 allowlist prompt values

### BlueBubbles（2026.4.18–20）

- **Send Timeout 提升至 30s**：修復 macOS 26 setups 靜默丟失 iMessage sends 的問題；可透過 `channels.bluebubbles.sendTimeoutMs` 配置
- **SSRF 修復**：通過 `BlueBubblesClient` 集中管理出站 HTTP，修復 localhost 圖片附件和 private-IP 反應 SSRF 阻塞
- **Method 明確設定**：出站文字發送明確設定 `method`，修復 macOS 26 AppleScript `-1700` 錯誤
- **iMessage 優先**：同一 handle 同時有 iMessage 和 SMS 時優先使用 iMessage
- **Tapback Fallback**：agent 用不在 tapback 集的 emoji 反應時 fallback 至 `love`
- **群組 System Prompt**：每群組 `systemPrompt` 配置注入 inbound context `GroupSystemPrompt`

### Matrix（2026.4.18–20）

- **sessions_spawn --thread 修復**：thread binding 建立、cleanup 和 completion-message delivery 修復
- **Allowlist 熱重載**：`dm.allowFrom` 和 `groupAllowFrom` 條目熱重載，不再需要重啟頻道
- **Mention Prefix 命令**：識別以 bot Matrix mention 為前綴的 slash commands（如 `@bot:server /new`）
- **私網 Homeserver**：遵守 `channels.matrix.network.dangerouslyAllowPrivateNetwork`

### Browser / CDP（2026.4.18–20）

- **WSL-to-Windows Chrome**：Remote CDP profile host 可用於 health checks，不擴大 SSRF policy
- **Phase-specific 診斷**：CDP readiness diagnostics 顯示失敗在哪個 phase（HTTP/WS discovery、SSRF validation、Browser.getVersion）
- **DevToolsActivePort**：attach failures 顯示為 browser-connectivity errors，而非 generic "waiting for tabs" timeout

### Mattermost（2026.4.20）

- **串流預覽**：thinking、tool activity 和 partial reply text 串流至單一 draft preview post，最終確定後就位

### Feishu（2026.4.18–20）

- **Card Action Chat Type**：從 Feishu chat API 解析 card-action chat type，DM 來源 card actions 不再繞過 `dmPolicy`

### QQBot（2026.4.20）

- **SSRF Guard**：`uploadC2CMedia` 和 `uploadGroupMedia` 新增 SSRF guard

### Cron（2026.4.18–20）

- **Jobs State 分離**：`jobs-state.json` 分離 runtime 狀態，`jobs.json` 保持穩定
- **交付預覽**：`cron list/show` 顯示 server-side 解析的交付目標預覽
- **Delivery Policy 大量修復**：delivery prompt gate、dry-run preview、implicit message send tracking、`delivery.mode: "none"` 語意
- **Telegram 去重複修復**：去重複 key 改為每次 cron execution，修復 recurring Telegram announce runs 靜默跳過後續 sends

## 各頻道改進歷史（2026.4.21）

### Slack（2026.4.21）

- **Thread Aliases 保留修復**（#62947）：generic runtime sends 現在在呼叫者提供 `threadTs` 時正確保持在預期的 Slack thread；修復多 workspace 或自訂 thread routing 場景下回覆落在錯誤 thread 的問題

### Browser（2026.4.21）

- **ax<N> Accessibility Refs 即時拒絕**（#69924）：act path 中無效的 `ax<N>` accessibility refs 立即被拒絕，不再等待 browser action timeout，大幅縮短失敗回饋時間

## 各頻道改進歷史（2026.4.22–2026.5.2）

### WhatsApp（2026.4.22–23）

- **replyToMode**（2026.4.22）：可設定原生引用回覆
- **systemPrompt**：每 **群組** 與每 **私訊**（2026.4.22）；帳戶級 `channels.whatsapp.accounts.<id>.{groups,direct}` **整表取代**根設定；支援 `"*"` 萬用字元
- **Onboarding**：首次 setup **不強制載入 Baileys runtime dep 路徑**（2026.4.23 QuickStart）；登入流程可 **預備 stage WhatsApp runtime deps**
- **結構化內嵌安全**（2026.4.23）：vCard／聯絡人／位置等改 **柵欄 JSON**，不污染 inline 正文
- **Outbound 媒體正規化**（2026.4.23）：direct send 與 auto-reply 路徑統一

### Slack（2026.4.23）

- **MPIM**：多人群組 DM 歸類為 **群組** 語境
- **非 DM 表面**：抑制冗長 tool／plan「Working…」類 trace，避免洩漏到頻道

### Telegram（2026.4.22）

- **Markdown 圖片**（2026.4.23）：最終回覆路徑將 `![...](...)` 解析為 **出站媒體**（群組不再回落純連結）

### Discord（2026.4.23）

- **Slash 與通道政策**：見 `02-架構設計` 安全條款；回覆發送維持權限脈絡

### Teams（2026.4.23）

- **Audience token**：共用 Bot Framework audience 須 **`appid`／`azp` 對應已設定 App**

### QQBot（2026.4.23）

- **`/bot-approve`** 需 **framework 認證**

### Voice／Twilio／Realtime（2026.4.23）

- **OpenAI realtime**：session 設定完成後才問候／轉發緩衝音訊
- **Twilio**：非 allowlist 主叫者在 stream 建立前被拒絕

### Telegram（2026.4.24）

- **輪詢**：移除 persisted offset 的 **`getUpdates` startup preflight**，重啟不再自我衝突。  
- **重入**：同一程式內 **禁止重複長輪詢** 同一 bot token；對 **外部 duplicate poller** 給更清楚衝突診斷。  
- **Webhook**：驗證通過後 **先 ack** 再跑 middleware，慢回合較不易觸發 Telegram 重試（#71392）。

### Feishu（2026.4.24）

- **主題群**：**session key** 穩定化。  
- **串流卡片**：啟動失敗時 **backoff**。  
- **語音氣泡**：語意／TTS 路徑 **MP3 轉 Ogg/Opus** 再送原生音訊；一般 MP3 附件仍當檔案（#61249、#37868）。

### Google Meet（2026.4.23）

- 離開會議時 **掛斷 Twilio** 委派；Chrome realtime 橋清理；**扁平** provider-safe tool schema

### Google Meet（2026.4.24）

- **Bundled participant 插件**：個人 Google OAuth、**Chrome／Twilio 即時會議**、**paired-node Chrome** 支援。  
- **Artifacts**：轉寫條目／出席等匯出；**預設對齊最新錄製**；新增 **最新會議**類指令與 **OAuth doctor**。

### Browser／自動化工具（2026.4.24）

- **座標點擊**、較長預設 **action 逾時**、**profile 級 headless 覆寫**；**navigation route 競態**改為略過良性錯誤（#68708）。  
- **Linux**：常見 Chromium 安裝路徑（`/opt/google`、`/opt/brave.com`、`/usr/lib/chromium`、`/usr/lib/chromium-browser`）先於手動 `browser.executablePath`（#48563）。  
- **ARIA 快照**：`format=aria` 時 **`axN` ref** 盡量綁 **live DOM**（Playwright 後端 id），利於後續 act（#62434）。  
- **Session 切換**：`/new`、`/reset`、閒置或每日換日前 **關閉舊 session 追蹤分頁**，防洩漏。

### Yuanbao／QQBot／Talk（2026.4.26）

- **騰訊元宝**：官方 **`openclaw-plugin-yuanbao`** 與 **`docs/channels/yuanbao.md`**（WebSocket DM／群組）（#72756）。  
- **QQBot**：**群組**完整語意、提及 gate、**FIFO**、**`stream_messages`**、chunked **`sendMedia`**、pipeline **`createEngineAdapters()`**（#70624）。  
- **Talk**：**瀏覽器 realtime transport** 抽象；**Google Live** ephemeral token；Gateway **relay**；changelog Fixes 強制 **WebSocket**、relay **會話上限**。

### Matrix（2026.4.26）

- **`openclaw matrix encryption setup`**：啟用 E2EE、bootstrap recovery、列印驗證狀態。

### 串流／Discord／WhatsApp／Meet（2026.4.27）

- **統一 progress**：**`streaming.mode: progress`**，**Discord／Telegram／Matrix／Slack／Microsoft Teams** 共用進度設定；Slack **`streaming.progress.render: rich`**（Block Kit）；**`streaming.preview.commandText`／`streaming.progress.commandText`** 可隱藏指令字面（**`status`**）。  
- **Discord**：**degraded transport**、**event-loop starvation** 訊號進 **`channels status`／`status --deep`**（#76327）；啟動 **不阻塞**於 bot probe（#77103）。  
- **WhatsApp**：**`@newsletter`** 外送目標（#13417）；**libsignal** `onlyBuiltDependencies`（#76539）；登入輸出 **走 runtime**。  
- **Google Meet**：預設 **`mode: "agent"`**（STT→agent→TTS）；**`bidi`**／**`realtime.strategy`** 保留雙向語音；**realtime intro** 可 **`""`** 靜音；**Twilio realtime** 與 **Gemini 橋**節流／背壓（#77064）；多項 **permission／buffer／session.updated** 就緒語意（見 Unreleased Fixes）。  
- **Matrix**：approval **reaction 目標先綁定**；工具進度與 **quiet preview** 行為對齊設定。

### TTS／語音（2026.4.25）

- **`/tts latest`** 朗讀與 **重複抑制**；**`/tts chat on|off|default`** 控制 **目前聊天**自動 TTS。  
- **Personas**：**`/tts persona`**；Gemini **`audio-profile-v1`**、OpenAI instruction 對應（#70748）。  
- **通道／帳號**：**Feishu**、**QQBot** 等支援 **`channels.<id>.accounts.<account>.tts`** 與全域／agent 設定的 **深度合併**。  
- **Agent**：**`agents.list[].tts`** 可覆寫 **`messages.tts`**。  
- **新 bundled 語音提供者**：**Azure Speech**、**小米 MiMo**、**Local CLI**、**Inworld**、**Volcengine／BytePlus**、**ElevenLabs v3** 等（詳見 CHANGELOG）。

### Discord（2026.4.25）

- **語音頻道 LLM**：**`channels.discord.voice.model`** 覆寫語音房所用模型（STT／TTS 仍依媒體設定）（#64368）。  
- **Health monitor**：失敗重啟計入 **冷卻與上限**，reload 時清除過期帳戶生命周期狀態（#38596）。

### Google Meet（2026.4.25）

- **行事曆**：**calendar-backed** 出席匯出流程、**manifest**、**dry-run** 預覽，與會議紀錄工具對齊。

### QA 通道（2026.4.23）

- **非 HTTP(S)** 入站附件 URL **拒絕與紀錄**

### 跨通道／providers／策略（2026.5.2 精選）

- **`threadBindings.spawnSessions`**：**取代** legacy subagent／ACP thread-spawn 分裂開關；**doctor --fix** 遷移（#75943）。  
- **Discord**：**access groups**；channel-audience DM **`accessGroup:<name>`**（#75813）；互動元件 **跨 Gateway 重啟**可用至過期；大量 **delivery／thread／PluralKit／voice／429／CDN upload** fixes（見 changelog）。  
- **Telegram**：forum **`/codex bind`**、native slash **topic session 檔**、**conversation bindings**、**長文 HTML chunk**、Bot API **逾時／typing** 等（見 changelog）。  
- **Slack**：**App Home** 預設 tab；**bot 參與 thread** 跨重啟（#11655）；DM／thread routing、directory **`directory self`**、缺 scope 錯誤保留等。  
- **WhatsApp**：**`@newsletter`** Channel／Newsletter 目標延續；**logout 經 Gateway**；Baileys **`end(error)`** teardown；quoted image **入站媒體**。  
- **BlueBubbles**：可選 **`replyContextApiFallback`**（預設 off）；全路徑 **SSRF** 三模式；log **password／token／Authorization** 脱敏（#71820）。  
- **Voice Call／Meet／Twilio**：DTMF／問候／realtime **階段日錄**；**`sessionScope: per-call`**（#45280）；CLI **`voicecall`** 委派 Gateway、operation-id polling（見 changelog）。  
- **使用者可見回覆**：strip **MiniMax／XML tool scaffolding**（#62820）；heartbeat strip legacy **`[TOOL_CALL]`** 區塊（#54138）。