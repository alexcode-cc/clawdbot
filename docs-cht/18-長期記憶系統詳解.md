# OpenClaw 長期記憶系統詳解

> 版本：`2026.5.3` | Node.js `>=22.16.0`
>
> 本文件完整說明 OpenClaw 的長期記憶系統架構、儲存格式、索引機制、搜尋功能、自動沖洗（flush）、Session 記憶 Hook，以及手動管理、清除和濃縮記憶的方法。

---

## 目錄

1. [記憶系統概述](#1-記憶系統概述)
2. [記憶檔案結構與格式](#2-記憶檔案結構與格式)
3. [記憶的建立方式](#3-記憶的建立方式)
4. [自動記憶沖洗（Memory Flush）](#4-自動記憶沖洗memory-flush)
5. [Session-Memory Hook](#5-session-memory-hook)
6. [記憶索引與搜尋系統](#6-記憶索引與搜尋系統)
7. [嵌入向量提供者](#7-嵌入向量提供者)
8. [混合搜尋（Hybrid Search）](#8-混合搜尋hybrid-search)
9. [Agent 記憶工具](#9-agent-記憶工具)
10. [QMD 進階後端](#10-qmd-進階後端)
11. [記憶管理與維護](#11-記憶管理與維護)
12. [記憶濃縮與清除策略](#12-記憶濃縮與清除策略)
13. [設定完整參考](#13-設定完整參考)
14. [CLI 指令參考](#14-cli-指令參考)
15. [注意事項與限制](#15-注意事項與限制)

---

## 1. 記憶系統概述

OpenClaw 的長期記憶系統是一個**以檔案為真相來源**的架構，使用 Markdown 檔案儲存記憶內容，上層以 SQLite（或 QMD）建立索引來實現語意搜尋。

### 核心原則

- **檔案即真相**：所有記憶儲存為磁碟上的 `.md` 檔案，索引僅為唯讀覆蓋層
- **兩層記憶**：精選的 `MEMORY.md`（長期）+ 日常的 `memory/YYYY-MM-DD.md`（日誌）
- **自動 + 手動**：系統自動沖洗記憶，使用者也可手動編輯
- **語意搜尋**：BM25 關鍵字 + 向量嵌入的混合搜尋

### 記憶來源

| 來源 | 說明 | 預設啟用 |
|------|------|---------|
| `memory` | Markdown 記憶檔案 | 是 |
| `sessions` | JSONL Session 逐字稿 | 否（experimental） |

---

## 2. 記憶檔案結構與格式

### 目錄結構

```
~/.openclaw/workspace/               # 工作區根目錄
├── MEMORY.md                        # 精選長期記憶（手動維護）
├── memory.md                        # 替代名稱（與 MEMORY.md 共存，去重）
└── memory/                          # 記憶目錄
    ├── 2026-03-20.md                # 每日記憶日誌（自動沖洗寫入）
    ├── 2026-03-21.md
    ├── 2026-03-22.md
    ├── 2026-03-22-api-design.md     # Session 記憶（Hook 自動建立）
    ├── 2026-03-23-bug-investigation.md
    └── topics.md                    # 主題分類記憶（手動建立）

~/.openclaw/agents/<agentId>/        # Agent 專屬目錄
├── <agentId>.sqlite                 # 記憶索引資料庫
└── sessions/                        # Session 逐字稿
    ├── session-abc123.jsonl
    └── session-def456.jsonl
```

### MEMORY.md — 精選長期記憶

**用途**：手動維護的精選記憶，包含重要的長期參考資訊。

**特性**：
- 在主要私人 session 中於啟動時載入
- 群組/頻道 context 中不載入
- Agent 記憶沖洗時**不會**修改此檔案（唯讀保護）
- 可由使用者或 Agent（透過明確指令）手動編輯

**建議內容**：
```markdown
# Long-term Memory

## 使用者偏好
- 使用繁體中文回覆
- 偏好簡潔的技術回覆
- 工作環境：Mac Mini M4 + Ubuntu Server

## 專案筆記
- 主要專案：OpenClaw 中文文件翻譯
- Git 分支策略：feature → develop → main

## 重要聯絡人
- @admin_user — 主要管理員
- @dev_team — 開發團隊頻道
```

### memory/YYYY-MM-DD.md — 每日記憶日誌

**用途**：自動記憶沖洗的目標檔案，追加式寫入。

**命名規則**：使用使用者設定的時區產生日期（如 `Asia/Taipei`）。

**特性**：
- **僅追加（Append-only）**：新內容追加至檔尾，不覆寫現有內容
- Session 啟動時載入今天 + 昨天的日誌
- 自動沖洗時由 Agent 決定寫入內容

**典型內容**：
```markdown
## 10:30 — API 設計討論

與 @dev 討論了新的 REST API 設計：
- 採用 /v2/resources 路徑
- 認證改用 Bearer token
- 分頁使用 cursor-based pagination

## 14:15 — 部署問題排查

Docker 容器啟動失敗，原因是 Node.js 版本不匹配：
- 將 Dockerfile 從 node:20 升級到 node:22
- 重新建置後問題解決

## 17:00 — 程式碼審查

審查了 PR #456：
- 修正了 memory flush 的追加邏輯
- 新增了 3 個單元測試
```

### memory/YYYY-MM-DD-slug.md — Session 記憶

**用途**：由 `session-memory` Hook 在 `/new` 或 `/reset` 時自動建立。

**命名規則**：`YYYY-MM-DD-{LLM 生成的 slug}.md`

**典型內容**：
```markdown
# Session: 2026-03-23 14:30:00 UTC

- **Session Key**: agent:main:telegram:direct:123456
- **Session ID**: abc123def456
- **Source**: telegram

## Conversation Summary

User: 幫我分析這個 API 的效能問題
Assistant: 分析了 /api/search 端點，發現 N+1 查詢問題...
User: 如何修復？
Assistant: 建議使用 DataLoader 批次查詢...
[... 最後 15 則訊息 ...]
```

---

## 3. 記憶的建立方式

OpenClaw 記憶可透過四種方式建立：

### 3.1 自動記憶沖洗（Memory Flush）

- **觸發時機**：Session 接近 context window 上限時，compaction 前自動執行
- **目標檔案**：`memory/YYYY-MM-DD.md`（追加）
- **行為**：Agent 靜默地將「持久性」資訊寫入記憶檔案
- 詳見 [第 4 節](#4-自動記憶沖洗memory-flush)

### 3.2 Session-Memory Hook

- **觸發時機**：使用者執行 `/new` 或 `/reset` 時
- **目標檔案**：`memory/YYYY-MM-DD-slug.md`（新建）
- **行為**：自動擷取前一 session 的最後 N 則訊息
- 詳見 [第 5 節](#5-session-memory-hook)

### 3.3 Agent 主動寫入

Agent 可透過 `write` 工具在對話中主動寫入記憶：

```
User: 記住我的偏好：回覆時使用繁體中文
Agent: [使用 write 工具寫入 memory/2026-03-23.md]
```

### 3.4 使用者手動編輯

使用者可直接編輯工作區中的 `.md` 檔案：
- 編輯 `MEMORY.md` 管理長期精選記憶
- 在 `memory/` 目錄新增或修改任何 `.md` 檔案
- 檔案監視器（watcher）會自動偵測變更並重新索引

---

## 4. 自動記憶沖洗（Memory Flush）

### 觸發條件

記憶沖洗在 **compaction（上下文壓縮）前** 自動觸發，條件為：

| 條件 | 預設值 | 說明 |
|------|--------|------|
| Token 軟閾值 | `contextWindow - reserveTokensFloor - 4000` | Session token 接近上限 |
| Transcript 大小 | 2 MB | Session 逐字稿超過 2 MB 強制觸發 |

**排除條件**（以下情況不觸發）：
- 工作區為唯讀（`ro`）或無存取（`none`）沙箱
- CLI provider run
- 心跳（heartbeat）session
- 同一 compaction 週期內已沖洗過

### 沖洗流程

```
1. 偵測到即將 compaction
        │
        ▼
2. 計算 context hash（最後 3 則訊息的 SHA-256）
   ├── 與前次相同 → 跳過（避免重複沖洗）
   └── 不同 → 繼續
        │
        ▼
3. 建構沖洗 prompt（含安全提示）
        │
        ▼
4. 以受限工具集執行 Agent turn
   ├── 僅允許 read + write 工具
   └── write 工具加上追加保護 wrapper
        │
        ▼
5. Agent 決定要記住什麼，寫入 memory/YYYY-MM-DD.md
        │
        ▼
6. Agent 回覆 NO_REPLY（靜默 turn）
        │
        ▼
7. 標記此 compaction 週期已沖洗
```

### 沖洗時使用的 Prompt

**系統 Prompt**：
```
Pre-compaction memory flush turn.
The session is near auto-compaction; capture durable memories to disk.
Store durable memories only in memory/YYYY-MM-DD.md (create memory/ if needed).
Treat workspace bootstrap/reference files such as MEMORY.md, SOUL.md, TOOLS.md,
and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them.
If memory/YYYY-MM-DD.md already exists, APPEND new content only and do not
overwrite existing entries.
You may reply, but usually NO_REPLY is correct.
```

**使用者 Prompt**：
```
Pre-compaction memory flush.
Store durable memories only in memory/YYYY-MM-DD.md (create memory/ if needed).
Treat workspace bootstrap/reference files such as MEMORY.md, SOUL.md, TOOLS.md,
and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them.
If memory/YYYY-MM-DD.md already exists, APPEND new content only and do not
overwrite existing entries.
Do NOT create timestamped variant files (e.g., YYYY-MM-DD-HHMM.md);
always use the canonical YYYY-MM-DD.md filename.
If nothing to store, reply with NO_REPLY.
```

### 安全保護機制

| 保護 | 說明 |
|------|------|
| **目標檔案限定** | 僅寫入 `memory/YYYY-MM-DD.md` |
| **Bootstrap 唯讀** | MEMORY.md、SOUL.md、TOOLS.md、AGENTS.md 不可修改 |
| **追加模式** | 已存在的日誌僅追加，不覆寫 |
| **工具限制** | 僅 `read` 和 `write` 工具可用 |
| **Write wrapper** | `write` 工具加上強制追加保護 |
| **無變體檔案** | 禁止建立 `YYYY-MM-DD-HHMM.md` 變體 |
| **去重** | 同一 compaction 週期不重複沖洗 |
| **Context hash** | 內容無變化時跳過沖洗 |

### 沖洗設定

```json5
{
  agents: {
    defaults: {
      compaction: {
        reserveTokensFloor: 20000,         // 預留 token 下限
        memoryFlush: {
          enabled: true,                   // 啟用自動沖洗
          softThresholdTokens: 4000,       // 軟閾值（tokens）
          forceFlushTranscriptBytes: 2097152, // 強制閾值（2 MB）
          // 自訂 prompt（需包含三項安全提示，否則自動附加）
          // systemPrompt: "...",
          // prompt: "...",
        },
      },
    },
  },
}
```

---

## 5. Session-Memory Hook

### 概述

`session-memory` 是一個 bundled hook，在 `/new` 或 `/reset` 時自動將前一 session 的對話摘要儲存為記憶檔案。

### 運作流程

```
1. 使用者執行 /new 或 /reset
        │
        ▼
2. Hook 觸發，讀取前一 session 的 JSONL 逐字稿
        │
        ▼
3. 擷取最後 N 則 user/assistant 訊息（預設 15 則）
        │
        ▼
4. 呼叫 LLM 從對話內容生成描述性 slug
   （失敗時 fallback 為時間戳 HHMM）
        │
        ▼
5. 建立檔案：memory/YYYY-MM-DD-{slug}.md
   （包含 session metadata + 對話內容）
```

### 設定

```json5
{
  hooks: {
    internal: {
      enabled: true,
      entries: {
        "session-memory": {
          enabled: true,
          messages: 15,           // 保留的訊息數（預設 15）
          llmSlug: true,          // 使用 LLM 生成檔名（預設 true）
        },
      },
    },
  },
}
```

### 啟用方式

```bash
# 透過 CLI 啟用
openclaw hooks enable session-memory

# 或在 onboard 精靈的 Hooks 步驟中選擇啟用
openclaw onboard
```

---

## 6. 記憶索引與搜尋系統

### 索引架構

```
Markdown 檔案 (.md)
      │
      ▼
  分塊 (Chunking)
  ├── 每塊 400 tokens
  └── 80 tokens 重疊
      │
      ▼
  嵌入向量 (Embedding)          全文索引 (FTS5)
  ├── Provider API 呼叫         ├── SQLite FTS5
  └── 儲存至 chunks_vec          └── 儲存至 chunks_fts
      │                              │
      └────────┬─────────────────────┘
               │
               ▼
         混合搜尋 (Hybrid Search)
         ├── 向量相似度 × 0.7
         ├── BM25 關鍵字 × 0.3
         ├── MMR 多樣性重排序（可選）
         └── 時間衰減（可選）
               │
               ▼
         排序結果 → Agent
```

### 索引資料庫

- **驅動**：SQLite（預設）
- **位置**：`~/.openclaw/agents/<agentId>.sqlite`（可設定）
- **表結構**：
  - `meta` — key-value 設定儲存
  - `files` — 已索引檔案（path、hash、mtime、size）
  - `chunks` — 文字分塊（含嵌入向量）
  - `chunks_vec` — 向量搜尋表（sqlite-vec 加速）
  - `chunks_fts` — 全文搜尋虛擬表（FTS5）
  - `embedding_cache` — 嵌入快取（按 provider/model/hash）

### 索引同步

| 觸發時機 | 設定 | 預設 |
|---------|------|------|
| Session 啟動時 | `sync.onSessionStart` | `true` |
| 搜尋時 | `sync.onSearch` | `true` |
| 檔案變更時 | `sync.watch` | `true`（debounce 1.5s） |
| 定期同步 | `sync.intervalMinutes` | `0`（停用） |

---

## 7. 嵌入向量提供者

### 支援的提供者

| 提供者 | 預設模型 | 維度 | 需要 API Key | Auto 偵測順序 |
|--------|---------|------|-------------|-------------|
| `local` | embeddinggemma-300m-qat-Q8_0 | — | 否 | 1（需設定 modelPath） |
| `openai` | text-embedding-3-small | 1536 | 是 | 2 |
| `gemini` | gemini-embedding-001 | 768/1536/3072 | 是 | 3 |
| `voyage` | voyage-4-large | — | 是 | 4 |
| `mistral` | mistral-embed | — | 是 | 5 |
| `ollama` | nomic-embed-text | — | 否 | 不自動選擇 |

### Auto 模式偵測

當 `provider: "auto"` 時，依序嘗試：
1. `local`（需 `memorySearch.local.modelPath` 已設定且檔案存在）
2. `openai`（需 OpenAI API Key 可用）
3. `gemini`（需 Gemini API Key 可用）
4. `voyage`（需 Voyage API Key 可用）
5. `mistral`（需 Mistral API Key 可用）
6. 全部不可用 → 記憶搜尋停用

### 提供者設定範例

```json5
{
  agents: {
    defaults: {
      memorySearch: {
        // 使用 OpenAI
        provider: "openai",
        model: "text-embedding-3-small",
        remote: {
          apiKey: "${OPENAI_API_KEY}",
        },

        // 或使用 Ollama（本地）
        // provider: "ollama",
        // model: "nomic-embed-text",
        // remote: {
        //   baseUrl: "http://localhost:11434",
        // },

        // 或使用本地模型（完全離線）
        // provider: "local",
        // local: {
        //   modelPath: "~/.openclaw/models/embeddinggemma-300m.gguf",
        //   modelCacheDir: "~/.openclaw/cache/models",
        // },

        // Fallback 提供者（主要不可用時使用）
        fallback: "local",              // 或 "none" 停用 fallback
      },
    },
  },
}
```

### 多模態嵌入

僅 Gemini embedding-2 支援圖片和音訊的嵌入：

```json5
{
  agents: {
    defaults: {
      memorySearch: {
        provider: "gemini",
        model: "gemini-embedding-2-preview",
        multimodal: {
          enabled: true,
          modalities: ["image", "audio"],    // 或 ["all"]
          maxFileBytes: 10485760,            // 10 MB
        },
        // 額外路徑（多模態檔案僅從 extraPaths 載入）
        extraPaths: ["~/photos/reference/"],
      },
    },
  },
}
```

**支援的多模態格式**：
- 圖片：`.jpg`、`.jpeg`、`.png`、`.webp`、`.gif`、`.heic`、`.heif`
- 音訊：`.mp3`、`.wav`、`.ogg`、`.opus`、`.m4a`、`.aac`、`.flac`

---

## 8. 混合搜尋（Hybrid Search）

### 搜尋流程

```
查詢文字
    │
    ├──────────────────┐
    ▼                  ▼
向量搜尋              BM25 關鍵字搜尋
(K × candidateMultiplier)    (K 筆結果)
    │                  │
    └────────┬─────────┘
             ▼
    合併（by chunk ID）
    加權計分：vector×0.7 + text×0.3
             │
             ▼
    MMR 多樣性重排序（可選）
    λ × relevance - (1-λ) × max_similarity
             │
             ▼
    時間衰減（可選）
    score × exp(-λ × ageInDays)
             │
             ▼
    返回 Top K 結果
```

### 搜尋設定

```json5
{
  agents: {
    defaults: {
      memorySearch: {
        query: {
          maxResults: 6,                    // 返回筆數
          minScore: 0.35,                   // 最低分數門檻

          hybrid: {
            enabled: true,                  // 啟用混合搜尋
            vectorWeight: 0.7,              // 向量權重（正規化後）
            textWeight: 0.3,                // 文字權重（正規化後）
            candidateMultiplier: 4,         // 候選倍數（重排序用）

            // MMR（最大邊際相關性）
            mmr: {
              enabled: false,               // 啟用多樣性重排序
              lambda: 0.7,                  // 0=最大多樣性，1=最大相關性
            },

            // 時間衰減
            temporalDecay: {
              enabled: false,               // 啟用時間衰減
              halfLifeDays: 30,             // 半衰期（天）
            },
          },
        },
      },
    },
  },
}
```

### 時間衰減規則

- **日期化檔案**（`memory/2026-03-20.md`）：依檔名日期計算年齡
- **常青檔案**（`MEMORY.md`、`memory/topics.md`）：**永不衰減**
- **衰減公式**：`score × exp(-ln(2)/halfLifeDays × ageInDays)`
- **效果**：halfLifeDays=30 時，30 天前的記憶分數減半

### 多語言支援

搜尋系統支援多語言的停用詞過濾和查詢擴展：

| 語言 | 支援 |
|------|------|
| 英語 | 完整停用詞列表 |
| 西班牙語 | 停用詞 |
| 葡萄牙語 | 停用詞 |
| 阿拉伯語 | 停用詞 |
| 韓語 | 助詞剝離 |
| 日語 | 特殊 N-gram 處理 |
| 中文 | 特殊 N-gram 處理 |

---

## 9. Agent 記憶工具

### memory_search — 語意搜尋

**功能**：對已索引的記憶進行語意搜尋。

**參數**：
```typescript
{
  query: string;          // 必要：搜尋查詢
  maxResults?: number;    // 可選：結果數量上限
  minScore?: number;      // 可選：最低分數門檻
}
```

**回傳結果**：
```typescript
{
  results: [
    {
      snippet: string;     // 約 700 字元摘要（含引用）
      path: string;        // 相對路徑（如 "memory/2026-03-23.md"）
      startLine: number;   // 起始行（1-indexed）
      endLine: number;     // 結束行
      score: number;       // 相關性分數（0-1）
      citation: string;    // "path#L123-L456"（citations 啟用時）
    },
    // ...
  ],
  provider: string;        // 使用的嵌入提供者
  model: string;           // 使用的嵌入模型
}
```

### memory_get — 目標讀取

**功能**：讀取特定記憶檔案的內容。

**參數**：
```typescript
{
  path: string;            // 必要：檔案路徑（如 "memory/2026-03-23.md"）
  from?: number;           // 可選：起始行（1-indexed）
  lines?: number;          // 可選：讀取行數
}
```

**回傳**：檔案內容（檔案不存在時回傳空字串，不拋錯）。

### 引用模式

| 模式 | 行為 |
|------|------|
| `auto`（預設） | 私人 DM 中顯示引用，群組/頻道中隱藏 |
| `on` | 總是顯示引用 |
| `off` | 總是隱藏引用（但 Agent 仍可用 path 呼叫 memory_get） |

---

## 10. QMD 進階後端

### 概述

QMD 是一個**實驗性**的外部側車二進位檔，提供 BM25 + 向量 + 重排序功能。Markdown 仍為真相來源，QMD 僅作為檢索引擎。

### 基本設定

```json5
{
  memory: {
    backend: "qmd",                        // "builtin"（預設）或 "qmd"
    qmd: {
      command: "qmd",                      // QMD 二進位路徑
      searchMode: "search",               // "search"（快速）| "vsearch"（向量）| "query"（慢，含擴展+重排序）
      includeDefaultMemory: true,          // 包含預設記憶目錄

      // 自訂索引路徑
      paths: [
        { path: "~/docs/notes", pattern: "**/*.md", name: "notes" },
        { path: "/srv/shared", name: "shared" },
      ],

      // Session 匯出
      sessions: {
        enabled: false,
        exportDir: "~/.openclaw/agents/{agentId}/qmd/sessions",
        retentionDays: 30,
      },

      // 更新排程
      update: {
        interval: "5m",                    // 更新間隔
        debounceMs: 15000,                 // 變更 debounce
        onBoot: true,                      // 啟動時同步
        waitForBootSync: false,            // 等待啟動同步完成
        embedInterval: "60m",              // 嵌入更新間隔
      },

      // 搜尋限制
      limits: {
        maxResults: 6,
        maxSnippetChars: 700,
        maxInjectedChars: 4000,
        timeoutMs: 4000,
      },
    },
  },
}
```

> QMD 失敗時自動 fallback 至內建 SQLite 後端。

---

## 11. 記憶管理與維護

### 日常管理

#### 查看記憶狀態

```bash
# 基本狀態
openclaw memory status

# 深度狀態（含嵌入探測）
openclaw memory status --deep

# 索引統計
openclaw memory status --index

# JSON 輸出（腳本用）
openclaw memory status --json
```

#### 手動重新索引

```bash
# 增量索引（僅變更的檔案）
openclaw memory index

# 強制完整重新索引
openclaw memory index --force

# 詳細日誌
openclaw memory index --verbose

# 指定 Agent
openclaw memory index --agent main
```

#### 搜尋測試

```bash
# 互動式搜尋
openclaw memory search "API 設計決策"

# 指定參數
openclaw memory search --query "部署問題" --max-results 10 --min-score 0.3

# JSON 輸出
openclaw memory search "Docker 設定" --json
```

### 記憶檔案的直接管理

由於記憶以 Markdown 檔案儲存，你可以直接操作：

```bash
# 查看所有記憶檔案
ls -la ~/.openclaw/workspace/memory/

# 查看特定日期的記憶
cat ~/.openclaw/workspace/memory/2026-03-23.md

# 編輯精選記憶
vim ~/.openclaw/workspace/MEMORY.md

# 查看 Session 記憶
cat ~/.openclaw/workspace/memory/2026-03-23-api-design.md
```

> 編輯後，檔案監視器會自動偵測變更並在 1.5 秒後重新索引。

---

## 12. 記憶濃縮與清除策略

### 清除舊記憶

#### 刪除特定日期的記憶

```bash
# 刪除單一日期
rm ~/.openclaw/workspace/memory/2026-01-15.md

# 刪除一個月前的所有記憶
find ~/.openclaw/workspace/memory/ -name "2026-01-*.md" -delete

# 刪除所有 Session 記憶（保留每日日誌）
find ~/.openclaw/workspace/memory/ -name "????-??-??-*.md" -delete

# 保留最近 30 天，刪除更舊的
find ~/.openclaw/workspace/memory/ -name "????-??-??.md" -mtime +30 -delete
```

#### 清空並重建索引

```bash
# 刪除索引資料庫（下次搜尋時自動重建）
rm ~/.openclaw/agents/main/main.sqlite

# 或強制重新索引
openclaw memory index --force
```

### 濃縮（Consolidation）策略

當記憶檔案過多或過大時，建議以下濃縮策略：

#### 策略 1：手動精選至 MEMORY.md

1. 瀏覽近期的 `memory/YYYY-MM-DD.md` 檔案
2. 將重要的長期資訊提取至 `MEMORY.md`
3. 刪除已整合的每日記憶

```bash
# 查看近期記憶
for f in ~/.openclaw/workspace/memory/2026-03-*.md; do
  echo "=== $(basename $f) ==="
  head -20 "$f"
  echo ""
done

# 編輯精選記憶，加入重要內容
vim ~/.openclaw/workspace/MEMORY.md

# 刪除已整合的來源
rm ~/.openclaw/workspace/memory/2026-03-{01..15}.md
```

#### 策略 2：按主題分類整理

在 `memory/` 目錄下建立主題檔案：

```
memory/
├── topics.md              # 主題索引
├── projects.md            # 專案筆記
├── preferences.md         # 偏好設定
├── contacts.md            # 聯絡人資訊
├── 2026-03-23.md          # 近期每日日誌
└── 2026-03-24.md
```

> 主題檔案（非日期命名）不受時間衰減影響。

#### 策略 3：請 Agent 協助濃縮

在對話中指示 Agent 進行濃縮：

```
User: 請閱讀 memory/ 目錄下 3 月 1 日到 15 日的所有記憶檔案，
將重要資訊整理成摘要，寫入 memory/2026-03-summary.md，
然後刪除原始的每日檔案。
```

#### 策略 4：啟用時間衰減

讓搜尋系統自動降低舊記憶的權重：

```json5
{
  agents: {
    defaults: {
      memorySearch: {
        query: {
          hybrid: {
            temporalDecay: {
              enabled: true,
              halfLifeDays: 30,    // 30 天半衰期
            },
          },
        },
      },
    },
  },
}
```

這不會刪除舊記憶，但搜尋時會優先返回近期記憶。

### 記憶大小監控

```bash
# 查看記憶目錄大小
du -sh ~/.openclaw/workspace/memory/

# 查看索引資料庫大小
du -sh ~/.openclaw/agents/main/main.sqlite

# 查看檔案數量
ls ~/.openclaw/workspace/memory/ | wc -l

# 按大小排序記憶檔案
ls -lhS ~/.openclaw/workspace/memory/
```

### 自動清除腳本範例

建立一個定期清除的 cron 腳本：

```bash
#!/bin/bash
# cleanup-memory.sh - 保留最近 60 天的每日記憶

MEMORY_DIR="$HOME/.openclaw/workspace/memory"
DAYS_TO_KEEP=60

# 找到並刪除超過 60 天的每日記憶（YYYY-MM-DD.md 格式）
find "$MEMORY_DIR" -maxdepth 1 -name "????-??-??.md" -mtime +$DAYS_TO_KEEP -delete

# 找到並刪除超過 90 天的 Session 記憶（YYYY-MM-DD-slug.md 格式）
find "$MEMORY_DIR" -maxdepth 1 -name "????-??-??-*.md" -mtime +90 -delete

echo "Memory cleanup completed: $(date)"
```

搭配 cron 每週執行：
```bash
# crontab -e
0 3 * * 0 /path/to/cleanup-memory.sh >> /tmp/memory-cleanup.log 2>&1
```

---

## 13. 設定完整參考

### memorySearch 完整設定

```json5
{
  agents: {
    defaults: {
      memorySearch: {
        // 基本設定
        enabled: true,                         // 啟用記憶搜尋
        sources: ["memory"],                   // "memory" | "sessions"
        extraPaths: [],                        // 額外記憶路徑

        // 嵌入提供者
        provider: "auto",                      // auto | openai | gemini | local | voyage | mistral | ollama
        model: undefined,                      // 提供者預設模型
        outputDimensionality: undefined,       // Gemini 專用維度
        fallback: undefined,                   // 備用提供者（或 "none"）

        // 遠端 API
        remote: {
          baseUrl: undefined,                  // 自訂端點
          apiKey: undefined,                   // API Key 或 SecretRef
          headers: {},                         // 自訂 headers
          batch: {
            enabled: false,
            wait: false,
            concurrency: 3,
            pollIntervalMs: 5000,
            timeoutMinutes: 30,
          },
        },

        // 本地模型
        local: {
          modelPath: undefined,                // GGUF 模型路徑
          modelCacheDir: undefined,            // 模型快取目錄
        },

        // 多模態
        multimodal: {
          enabled: false,
          modalities: [],                      // "image" | "audio" | "all"
          maxFileBytes: 10485760,              // 10 MB
        },

        // 儲存
        store: {
          driver: "sqlite",
          path: undefined,                     // 預設 ~/.openclaw/agents/{agentId}.sqlite
          vector: {
            enabled: true,
            extensionPath: undefined,          // sqlite-vec 路徑
          },
        },

        // 分塊
        chunking: {
          tokens: 400,                         // 每塊 token 數
          overlap: 80,                         // 重疊 tokens
        },

        // 同步
        sync: {
          onSessionStart: true,
          onSearch: true,
          watch: true,
          watchDebounceMs: 1500,
          intervalMinutes: 0,                  // 0=停用
          sessions: {
            deltaBytes: 102400,                // 100 KB
            deltaMessages: 50,
            postCompactionForce: true,
          },
        },

        // 搜尋
        query: {
          maxResults: 6,
          minScore: 0.35,
          hybrid: {
            enabled: true,
            vectorWeight: 0.7,
            textWeight: 0.3,
            candidateMultiplier: 4,
            mmr: { enabled: false, lambda: 0.7 },
            temporalDecay: { enabled: false, halfLifeDays: 30 },
          },
        },

        // 快取
        cache: {
          enabled: true,
          maxEntries: undefined,               // 無上限
        },

        // 引用
        citations: "auto",                     // auto | on | off

        // 實驗性
        experimental: {
          sessionMemory: false,                // 索引 session 逐字稿
        },
      },
    },
  },
}
```

### memory 後端設定

```json5
{
  memory: {
    backend: "builtin",                        // builtin | qmd
    citations: "auto",
    qmd: { /* 見第 10 節 */ },
  },
}
```

### compaction memoryFlush 設定

```json5
{
  agents: {
    defaults: {
      compaction: {
        reserveTokensFloor: 20000,
        memoryFlush: {
          enabled: true,
          softThresholdTokens: 4000,
          forceFlushTranscriptBytes: 2097152,
          systemPrompt: undefined,              // 自訂系統 prompt
          prompt: undefined,                    // 自訂使用者 prompt
        },
      },
    },
  },
}
```

---

## 14. CLI 指令參考

```bash
# ─── 記憶狀態 ───
openclaw memory status                    # 基本狀態
openclaw memory status --deep             # 含嵌入探測
openclaw memory status --index            # 索引統計
openclaw memory status --json             # JSON 輸出
openclaw memory status --agent <id>       # 指定 Agent
openclaw memory status --verbose          # 詳細

# ─── 索引管理 ───
openclaw memory index                     # 增量索引
openclaw memory index --force             # 完整重建
openclaw memory index --agent <id>        # 指定 Agent
openclaw memory index --verbose           # 詳細

# ─── 搜尋 ───
openclaw memory search "查詢文字"
openclaw memory search --query "text" --max-results 10 --min-score 0.3
openclaw memory search --json             # JSON 輸出
openclaw memory search --agent <id>

# ─── Hooks 管理 ───
openclaw hooks enable session-memory      # 啟用 Session 記憶
openclaw hooks disable session-memory     # 停用
openclaw hooks info session-memory        # 查看詳情

# ─── 設定管理 ───
openclaw config get agents.defaults.memorySearch
openclaw config set agents.defaults.memorySearch.enabled true
openclaw config set agents.defaults.memorySearch.provider openai
openclaw config set agents.defaults.compaction.memoryFlush.enabled true
```

---

## 15. 注意事項與限制

### 重要須知

1. **記憶非揮發性**：所有記憶必須寫入磁碟才能持久化，RAM 中的資訊在 session 結束後遺失
2. **分塊以 token 計**：搜尋結果的行號對應原始檔案行號，非分塊邊界
3. **Session 索引需 opt-in**：`experimental.sessionMemory = true` 才會索引 session 逐字稿
4. **多模態僅限 Gemini**：僅 `gemini-embedding-2-preview` 支援圖片/音訊嵌入
5. **QMD 完全可選**：內建 SQLite 已可生產使用，QMD 為實驗性替代方案
6. **sqlite-vec 為可選加速**：不可用時 fallback 至記憶體內餘弦相似度
7. **FTS5 依賴 SQLite 編譯選項**：不可用時關鍵字搜尋優雅降級
8. **沖洗每 compaction 週期最多一次**：防止重複沖洗
9. **檔案監視 debounce**：快速連續編輯在 1.5 秒內合併為一次重新索引
10. **嵌入需要 API Key**：遠端提供者需要對應的 API Key，本地提供者不需要

### 效能考量

| 項目 | 建議 |
|------|------|
| 記憶檔案數量 | 數百個檔案可正常運作，數千個建議定期清理 |
| 單檔大小 | 建議 < 1 MB（索引上限依設定而定） |
| 索引資料庫 | 通常 < 100 MB，可安全刪除重建 |
| 嵌入 API 呼叫 | 首次索引可能較慢，後續增量快取 |
| 搜尋延遲 | 通常 < 1 秒（sqlite-vec 加速時） |

### 常見問題

| 問題 | 解決方案 |
|------|---------|
| 記憶搜尋無結果 | 確認 `memorySearch.enabled: true`，檢查 `openclaw memory status --deep` |
| 嵌入失敗 | 確認 API Key 有效，執行 `openclaw memory status --deep` |
| 索引過時 | 執行 `openclaw memory index --force` |
| 記憶檔案過多 | 見 [第 12 節](#12-記憶濃縮與清除策略) 清除策略 |
| Session 記憶未建立 | 確認 `session-memory` hook 已啟用 |
| 沖洗未觸發 | 確認 `compaction.memoryFlush.enabled: true` |
| 沖洗覆寫了記憶 | 確認使用最新版本（追加保護已內建，2026.3.28 修復） |
| CJK 搜尋結果不佳 | 確認使用 2026.3.28+（CJK chunking 和 MMR tokenize 已修復） |

### CJK 記憶支援改善（2026.3.28+）

自 2026.3.28 起，記憶系統對中日韓語言有以下重大改善：

1. **QMD 分塊**：正確計算 CJK 字元（中/日/韓/越南語漢字）的 token 數量，避免分塊過大或過小
2. **UTF-16 代理對保護**：fine-split 不再切斷 UTF-16 surrogate pairs（Emoji 等擴充字元安全）
3. **MMR 多樣性**：`tokenize()` 正確分詞 CJK/Kana/Hangul 字元，多樣性重排序不再偏好拉丁語系
4. **Memory Flush 追加修復**：修復 2026.3.24-3.27 版本中可能覆寫（而非追加）每日記憶檔案的回歸

---

### Dreaming 記憶系統（2026.4.5+）

自 2026.4.5 起，OpenClaw 引入全新的 **Dreaming 記憶系統**，模擬人類睡眠中的記憶整理過程：

#### 核心概念

| 階段 | 說明 |
|------|------|
| **Sleep Phases** | 多階段記憶推廣流程（NREM → REM → Deep Sleep） |
| **Weighted Recall** | 基於權重的記憶回憶機制，近期和高頻記憶優先 |
| **REM Preview** | REM 階段預覽，在深度處理前預覽待推廣記憶 |
| **Aging Controls** | 記憶老化控制，低權重記憶隨時間淡化 |
| **DREAMS Trail** | 記憶推廣軌跡記錄，完整追蹤記憶處理過程 |

#### 運作方式

1. **收集**：對話中自動標記有價值的資訊片段
2. **暫存**：片段暫存於短期記憶緩衝區
3. **Sleep 觸發**：Session 閒置或 compaction 時觸發 dreaming 流程
4. **分階段處理**：
   - NREM：初步過濾與分類
   - REM：關聯性分析和記憶連結
   - Deep Sleep：長期記憶寫入與重複去除
5. **推廣**：符合條件的記憶推廣至 `memory/` 目錄下的持久檔案

#### 設定

```json5
{
  agents: {
    defaults: {
      dreaming: {
        enabled: true,
        // Sleep phase 觸發間隔（預設 idle 觸發）
        aging: {
          enabled: true,
          halfLifeDays: 7  // 記憶半衰期（天）
        }
      }
    }
  }
}
```

### Bedrock 嵌入提供者（2026.4.5+）

新增 Amazon Bedrock 作為嵌入向量提供者，支援：
- `amazon.titan-embed-text-v2:0`
- AWS SDK 認證（同 Bedrock LLM 提供者）
- 設定：`agents.defaults.memorySearch.provider: "amazon-bedrock"`

### 每日記憶分塊改善（2026.4.5+）

- 改善每日記憶檔案的分塊策略，對長對話紀錄更準確
- 記憶索引支援增量更新，減少不必要的全量重建
- 記憶搜尋結果排序考量記憶新鮮度（recency bias）

### Memory Wiki 系統（2026.4.8+）

自 2026.4.8 起，OpenClaw 在 Dreaming 記憶基礎上建立 **Memory Wiki** 知識管理層：

#### 核心概念

| 元素 | 說明 |
|------|------|
| **Belief-layer Digests** | 信念層摘要，將記憶片段聚合為結構化知識摘要 |
| **Claim Health Reports** | 知識聲明健康報告，追蹤知識片段的可信度和新鮮度 |
| **Compiled Digest Prompts** | 經過編譯的摘要提示詞，用於 context engine 注入 |
| **Digest Retrieval** | 搜尋時使用 compiled digests 提升檢索精確度 |

#### 運作方式

1. **記憶聚合**：Dreaming 推廣的記憶片段被聚合為結構化的 digest
2. **Belief 追蹤**：每個 digest 記錄來源 claims，追蹤可信度
3. **Health 監控**：定期產生 claim health reports，標記過時或衝突的知識
4. **Context Engine 整合**：compiled digest prompts 被注入 context engine，提升回覆品質
5. **向後相容**：支援從舊版記憶格式的無縫遷移

#### 設定

```json5
{
  agents: {
    defaults: {
      memoryWiki: {
        enabled: true,
        digestCompilation: true,
        claimHealth: true
      }
    }
  }
}
```

### Dreaming 改善（2026.4.8+）

- **Slot-aware 配置**：Dreaming 配置正確區分不同 memory slot，多 agent 環境下互不干擾
- **Session 攝取改善**：新增時間戳分桶（timestamp bucketing）和游標式 session 攝取
- **隱私與冪等性**：加強 dreaming session 攝取的隱私保護和冪等性
- **解耦 memorySearch**：dreaming session 攝取不再依賴 memorySearch 設定
- **Daily Ingestion 修復**：日常記憶攝取忽略 managed dreaming blocks，避免重複處理
- **Context Engine Memory Helper**：context engine 新增 memory prompt helper

---

## Active Memory Recall 插件（2026.4.12）

### 概述

Active Memory Recall 成為獨立插件，提供主動式記憶召回功能：

- **QMD Recall-to-Search**：透過 QMD（Query-Match-Dispatch）機制，將記憶召回請求轉換為搜尋查詢
- **`/trace` 診斷**：TUI 中新增 `/trace` 指令，即時切換記憶召回追蹤模式
- **Lexical Fallback 改善**：詞彙回退搜尋改善，在嵌入搜尋效果不佳時提供更可靠的結果

### Grounded REM（2026.4.12）

Dreaming 系統引入 Grounded REM（紮根快速眼動記憶）：

- **Hardened REM Extraction**：強化 REM 萃取流程，確保記憶品質
- **Backfill Lane**：新增回填通道，補充遺漏的記憶片段
- **Workspace Isolation**：workspace 隔離確保多 agent 環境下記憶不互相污染
- **Belief-layer Decoupling**：信念層解耦，將事實記憶與推論分離

### Memory Wiki Unicode 修復

修復 Memory Wiki 中的 Unicode 處理問題，確保多語言內容正確儲存和檢索。

---

## 記憶系統改善（2026.4.13–2026.4.14）

### Active Memory Recall 位置調整

自 2026.4.14 起，Active Memory Recall 移入**隱藏的 prompt prefix**：

- Recall 結果不再出現在可見的對話歷史中
- 仍在 agent context 中發揮作用，但對使用者不可見
- 減少 context 噪音，提升對話品質

### Embedding Proxy Provider 修復

修復 embedding proxy provider 前綴保留問題（#66452）：

- 正規化 OpenAI 相容 embedding 模型引用時正確保留非 OpenAI provider 前綴
- 修復 proxy-backed memory providers 顯示 `Unknown memory embedding provider` 的錯誤
- 受影響的使用者：使用代理模式（如 LiteLLM、One-API 等）的 embedding 配置

### Ollama Embedding Adapter 恢復

修復 Ollama embedding adapter 停止工作的問題（#66269）：

- 恢復 Ollama 本地嵌入向量功能
- 確保本地記憶嵌入索引正常建立

### Dreaming 頻率修復

修復 Dreaming 在每個 cron schedule 中執行多次的問題（#66139）：

- 每個 cron schedule 只執行一次 Dreaming
- 避免重複記憶促進和不必要的計算資源消耗

### 預設根記憶體處理統一

統一預設根記憶體處理方式（#66141），確保 `MEMORY.md`、`memory.md` 和 `memory/` 目錄的行為一致。

---

## 記憶系統改善（2026.4.15）

### Dreaming 儲存模式預設改變（重要）

自 2026.4.15 起，`dreaming.storage.mode` 預設從 `inline` 改為 **`separate`**：

- Dreaming phase blocks（`## Light Sleep`、`## REM Sleep`）儲存在 `memory/dreaming/{phase}/YYYY-MM-DD.md`
- **不再**注入至 `memory/YYYY-MM-DD.md` 每日記憶文件
- 每日記憶文件不再被大量 phase-block 行主導
- 每日攝入掃描器不再需要與數百行 phase-block 競爭
- 若需舊行為：`plugins.entries.memory-core.config.dreaming.storage.mode: "inline"`

### Dreaming 自我攝入阻止

修復普通對話記錄引用 dream-diary prompt 被錯誤分類為內部 dreaming runs 的問題，確保這些對話記錄不會從 session recall 攝入中靜默丟棄。

### Dreaming 每日桶（dayBucket）使用攝入日期

修復使用來源文件日期作為每日召回去重的問題，現在使用**攝入日期**：
- 同一每日筆記的重複掃描可跨日增加 `dailyCount`
- 修復 dayBucket 在 1 停滯的問題

### Session Corpus 元資料清理

在正規化前從 session-corpus 使用者 turn 中去除 AI 面向的入站 metadata 信封，確保 REM topic 萃取看到使用者的實際訊息文字（包括陣列形狀的 split envelopes）。

### Dreaming Transcript 攝入修復

在 session-store 元資料 bootstrap 記錄到來前跳過 dreaming narrative transcripts，防止 dream diary 提示/散文行污染 session 攝入過程。

### Memory/QMD 讀取收緊

`memory_get` 只能讀取 canonical 記憶文件（`MEMORY.md`、`memory.md`、`DREAMS.md`、`memory/**`），不能作為通用工作區文件讀取繞過工具政策。

### GitHub Copilot Embedding Provider

GitHub Copilot 新增 embedding provider 用於記憶搜尋，支援 Copilot-backed memory search，並提供專用的 host helper 供插件重用 transport。

### Memory LanceDB 雲端儲存

`memory-lancedb` 插件新增雲端儲存支援，持久化記憶索引可在遠端物件儲存（S3/GCS 等）運行。

### Context Window 記憶 Excerpt 收緊

`memory_get` excerpts 預設設有 cap，包含明確的 continuation metadata，確保長 session 預設拉取更少 context，同時不失去確定性的後續讀取。

## 記憶系統改善（2026.4.18–2026.4.20）

### Active Memory 優雅降級（2026.4.20）

記憶回憶現在優雅降級（#69485）：
- Recall 在 prompt 建置期間失敗時記錄 warning，繼續回覆
- 不再因記憶回憶失敗而中斷整個 turn
- Active Memory recall timeout 上限提升至 **120 秒**（#68480）

### Dreaming Narrative Session 洩漏修復（2026.4.18）

修復 Dreaming narrative sub-sessions 洩漏的問題（#67023）：
- 正規化 sweep timestamps
- 重用 hashed narrative session keys 進行 fallback cleanup

### sqlite-vec 警告改善（2026.4.18）

每次降級 episode 只發出一次 sqlite-vec 降級警告（#67898）：
- 不再為每次文件寫入重複發出
- 跨 safe-reindex rollback 保持 latch

### Memory-core 向量維度保留（2026.4.18）

read-only 恢復期間保留儲存的 vector dimensions：
- 防止 memory indexes 在修復 read-only 狀態時丟失 vector metadata

## 記憶系統改善（2026.4.22–2026.5.3）

### 本機嵌入上下文（2026.4.23）

- 新增可設定 **`memorySearch.local.contextSize`**，**預設 4096**，便於在記憶宿主上調整本地 embedding 視窗（#70544）

### Memory CLI 與 manifest（2026.4.23）

- **內建 `local` embedding provider** 寫入 memory-core manifest：`openclaw memory status`／`index`／`search` 可與 Gateway runtime 一致解析本機嵌入（#70873）

### sqlite-vec KNN（2026.4.22）

- Vector recall 改用 **sqlite-vec KNN**，同時保留多篇模型索引的 **完整 post-filter limit**（#69680）

### QMD 託管集合修復（2026.4.23）

- Startup repair 若發現託管 QMD **集合名已存在但狀態過期**，會 **重建**，使根記憶收窄回 `MEMORY.md` 為主，而非長期寬索引

### Root 記憶檔與 doctor（2026.4.23）

- 耐久根記憶以 **`MEMORY.md`** 為準；**小寫 `memory.md` 不再作為運行期後備**；`openclaw doctor --fix` 可將 true split-brain 根檔 **合併入 `MEMORY.md` 並備份**（#70621）

### Dreaming 排程（2026.4.23）

- Managed dreaming cron **與 heartbeat 解耦**（輕量隔離 agent turn）；`doctor --fix` 可遷移舊 cron 形狀（與 `10-配置系統` 條目對齊）

### Hybrid 檢索與可選宿主（2026.4.24）

- **Hybrid search**：可取得 **分量／元件分數**（除錯與調參）。  
- **Llama runtime**：記憶路徑上 **維持可選載入**，降低未安裝 llama 主機之情境誤報（#71425）。

### Skills／記憶監看（2026.4.25）

- **Chokidar v5**：改監看 **具體 skill／memory 根目錄 + filter**，含 **SKILL.md 刪除**與資料夾移除，避免過度遞迴 workspace（#27404、#33585、#41606）。

### Memory search 嵌入選項（2026.4.26）

- OpenAI-compatible 路徑可設定 **`memorySearch.inputType`／`queryInputType`／`documentInputType`**（非對稱嵌入）；詳見 **`10-配置系統`** 與英文 memory 文件。

### LanceDB／status 路徑（2026.4.27）

- **LanceDB**：bundled 插件宣告 **`apache-arrow`** peer，避免向量維度／runtime 缺失（#76910）。  
- **`memory status`**：維持 **`--deep`／`--index`** 才做較重 probe；plain **`memory status`** 走 **便宜唯讀路徑**（#76769）。

### 2026.5.2（精選）

- **Markdown／Dreaming**：Managed 區塊 **CRLF 就地替換**、**重複 marker** 折疊而不破壞非託管內容（#75491 等）。  
- **Wiki links**：驗證接受 **相對鏈結含 `.md` 後綴**（減 false positive）（見 changelog Fixes）。  
- **SQLite／Windows**：原子 reindex **短暫鎖**重試（#64187）。  
- **sessions.json**：不再持久化每 session **`skillsSnapshot.resolvedSkills`** 巨型陣列（冷啟由 disk **rehydrate**）（#11950 脈絡）。  
- **Dreaming sweep**：納入 **主要 runtime workspace** 且不將 **main transcript** 混入 subagent workspace（#70014）。

### 2026.5.3（精選）

- **Transcript archive 搜尋**：**`.jsonl.reset.*`／`.jsonl.deleted.*`** 內容可索引並映射回 live stem（#56131 脈絡）。  
- **sqlite-vec**：packaged **optional**；缺 extension 時 recovery 指向有效 **`vector.extensionPath`**。  
- **Embedding reindex**：**`fetch failed`／`ECONNRESET`／`socket hang up`／`UND_ERR_*`／`closed`** 等 **短暫錯誤可重試**（#76311）。  
- **`memory status --deep`**：**向量店就緒**與 **embedding provider** 診斷 **分拆**（避免誤判）。  
- **File watcher 上限**：builtin memory auto-sync 視為 **非致命**（#73357）。

*本文件基於 OpenClaw `2026.5.3` 版本撰寫。完整英文文件請參考 https://docs.openclaw.ai/concepts/memory*
